# 验证记录（M0 / M1 / M2）

本文件记录「Tauri 能不能跑起这套桌宠」（M0）、「宠物本体补齐」（M1）与
「像个正经应用」（M2）的验证**方法**与**原始证据**。
所有结论都来自本机实跑，不是推断；证据取自诊断日志 `pet-debug.log` 与窗口枚举结果。
M2 从第 9 节开始。

验证环境：

| 项 | 值 |
| --- | --- |
| 系统 | Windows 11 专业版 10.0.26200 |
| 显示器 | 单屏 2560×1392（工作区，含任务栏扣除） |
| 工具链 | Rust 1.98.1 / `x86_64-pc-windows-gnu`，Tauri 2.11.5，WebView2 153.0.4234.32 |
| 运行方式 | `pnpm tauri dev`（前端走 Vite dev server） |

---

## 1. 透明置顶小窗

**方法**：进程启动后用 `EnumWindows` + `GetWindowThreadProcessId` 找本进程的顶层窗口，
读 `GetWindowRect` 与 `GetWindowLong(GWL_EXSTYLE)`。

**证据**（初始定位修复前的枚举结果；修复后的窗口原点见第 2 节）：

```
2885604|Tauri Window|Tauri App|1720,0,840x656|vis=True|ex=0x40118
```

**判读**：

- 尺寸 `840×656` = 宠物宽 `420` + 外扩 `210×2`（宽），宠物高 `420×9/16=236` + `210×2 = 656`（高）
  → 与 `WINDOW_MARGIN_RATIO = 0.5` 的设计一致；
- `ex=0x40118` 含 `WS_EX_LAYERED(0x00080000)`（透明必需）与 `WS_EX_TOOLWINDOW(0x00000080)`
  （不显示在 Alt+Tab / 任务栏，`skip_taskbar` 生效）；
- 窗口位置在修复后为 `(1906,-110)`（允许越出屏幕上缘，见第 2 节）——**窗口位置本身不是最终判据**，
  真正要判的是"宠物包围盒落在配置的位置上"，见第 2 节。


> **重要限制**：`PrintWindow` 抓不到 WebView2 的合成内容（返回全黑），
> 这是 DirectComposition 的已知行为，不是渲染失败。
> 因此"宠物真的画出来了"只能靠**行为证据**（下面几节）与窗口样式来判定，
> 不能靠本环境里的截图。

---

## 2. 配置下发、初始定位与坐标换算

**方法**：前端在启动阶段把关键量写进诊断日志（`pet-debug.log`）。

**证据**（初始定位 bug 修复后的实测）：

```
启动: 页面加载完成，assetBaseUrl=http://pet.localhost
配置: size=420 idle="待机呼吸休闲.webm" click="点击回应-元气挥手.webm" hitBox=131,33 158x187
窗口: 初始 原点=(1906,-110) 尺寸=840x656 → 包围盒=(2116,100)
```

**判读**：

- `hitBox=131,33 158x187`：由 `HIT_BOX(200,50)-(440,335)` 在 640×360 画布上按 `420` 宽等比换算——
  `200/640*420 = 131.25`，`50/360*236.25 = 32.8`，宽 `240/640*420 = 157.5`，高 `285/360*236.25 = 187.0` → 全部吻合；
- 包围盒 `(2116,100)` **正好等于配置语义**：右上角 + `marginX=24` → `x = 2560 - 420 - 24 = 2116`；
  `marginY=100` → `y = 0 + 100 = 100`；
- 窗口原点 `(1906,-110)` = 包围盒 − 外扩余量 `210`，**y 为负是设计使然**：
  窗口上缘越出屏幕的那 110px 只是透明外扩余量（不可见、点击穿透），
  换来的是"宠物严格落在配置的边距上"这一确定语义。

> **修复记录（M0 期间发现并修掉的一个真实 bug）**
>
> 早期实现按**窗口矩形**做工作区夹取，导致窗口被顶回工作区左上角、宠物被整整推出去
> 一个外扩余量——实测配置 `marginY=100` 的宠物出现在 `y=210`。
> 现在改为：**夹取约束作用在宠物包围盒上**，窗口原点恒等于"包围盒 − 外扩余量"
> （见 `pet_window.rs::anchored_window_origin` 的注释）。
> 这条不变量是"宠物在屏幕上动、页面只做窗口内布局"这一分工的基础，必须守住。


---

## 3. 自定义协议与透明视频

**方法**：前端 `MediaBuffer` 在等待首帧成功/失败时写日志。

**证据**：

```
动画: 首帧就绪 readyState=4 src=http://pet.localhost/webm/%E5%BE%85%E6%9C%BA%E5%91%BC%E5%90%B8%E4%BC%91%E9%97%B2.webm
动画: 切换完成 mode=loop readyState=1 尺寸=640x360 url=...
```

**判读**：

- `http://pet.localhost/...` 即 Windows 上自定义协议 `pet://` 的表现形式（平台差异已按预期处理）；
- 中文文件名经百分号编码后能正确解析（路径解码链路通）；
- `尺寸=640x360` 说明视频**真的被解码了**（`videoWidth/videoHeight` 来自解码后的元数据）；
- `readyState` 在 `switchTo` 里被 `currentTime = 0` 的重置动作打回 `1`：
  **这是日志可读性问题，不影响播放**（首帧就绪那一刻已是 `4`）。

---

## 4. 点击穿透自愈（本项最关键）

**方法**：用 `SetCursorPos` 把系统光标移进/移出宠物身体，读命中与翻转日志。

**证据**：

```
交互: 窗口切换为 可交互        ← 光标进入身体
交互: 窗口切换为 点击穿透      ← 光标移开
命中: 首次进入身体 光标=(2140,336) 包围盒=(1930,210)
```

**判读**：

- `(2140,336)` 换算到包围盒内 = `(210,126)`，落在命中框 `131,33 + 158x187` → `x∈[131,289]`、`y∈[33,220]` 之内 → 判定正确；
- 进入 → 可交互、离开 → 穿透，**双向翻转都生效**；
- 这一条验证了 Tauri 下唯一可行的穿透方案（Rust 轮询全局光标 + 前端精确判定）确实工作。

---

## 5. 点击回应

**方法**：`SetCursorPos` 进入身体后 `mouse_event` 左键按下+抬起。

**证据**：

```
拖拽: 结束
输入占用: 结束（恢复兜底翻转）
点击: 位移未超阈值，按点击处理
动画: 首帧就绪 readyState=4 src=...%E7%82%B9%E5%87%BB%E5%9B%9E%E5%BA%94-%E5%85%83%E6%B0%94%E6%8C%A5%E6%89%8B.webm
```

**判读**：按下→抬起之间位移未超 `DRAG_THRESHOLD(5px)`，正确归为"点击"并播放点击回应动画。

---

## 6. 拖拽弹簧跟手 + 甩抛物理 + 落地静止

**方法**：真实鼠标注入在本环境不可靠（探测发现 `SetCursorPos` 会被桌面上的其它窗口干扰，
`WindowFromPoint` 常落在别的进程上），因此改用**确定性自测钩子**：

```
set WHALE_PET_AUTOTEST=1 && pnpm tauri dev
```

它让页面调用 `PetRuntime.runSelfTest()`，用**合成的光标采样**（走的正是 Rust 光标通道喂帧的同一条代码路径）
驱动一次完整的按下 → 分步拖动 → 甩手松开 → 等待落地。

**轨迹设计说明（重要）**：自测的步长刻意取 **≈9px / 16ms**（贴近真实鼠标的事件粒度）。
早期版本用 60px/40ms 的"大跳变"，结果测得 872px 的弹簧滞后——那是**测试轨迹造成的假象**
（单帧位移远大于真实输入），不代表真实手感。换成真实粒度后滞后降到 121px（见下）。

**证据**：

```
自测: 开始 起点=(2326,226) 包围盒=(2116,100)
命中: 首次进入身体 光标=(2326,226) 包围盒=(2116,100)
交互: 窗口切换为 可交互
拖拽: 开始（已打断飞行与 Q 弹）
输入占用: 开始（Rust 侧停止兜底翻转）
自测: 拖拽中 包围盒=(1725,258) 位移=(-391,158) 弹簧滞后=(121,-68) 窗口回传累计=69 次
拖拽: 结束
输入占用: 结束（恢复兜底翻转）
松手: 甩出 速度=(-3269,1509)px/s 位置=(1688,273)
飞行: 开始，边界 1 块屏
飞行: 结束于 (1441,1156)，落地冲击 2173px/s
自测: 结束 最终包围盒=(1441,1156) 飞行中=false
自测: 窗口回传 总计=801 次，平均 63.3 次/秒（本次自测期间新增 800 次）
```

**判读**：

| 环节 | 实测值 | 判读 |
| --- | --- | --- |
| 弹簧跟手 | 指针走 `(270,90)`，包围盒走 `(391,158)`、**滞后 121px** | 滞后量约为一个命中区宽度（158px 的 77%），既"跟手"又保留重量感；若实现成"位置 = 指针"则滞后恒为 0 |
| 初速估算 | 甩手轨迹 `40px/16ms` → 估算 `(-3269,1509)`，合成 `3599px/s` | 与"软钳速上限 3600"几乎相等 → 软钳速与力度缩放链路生效 |
| 逐屏边界 | `飞行: 开始，边界 1 块屏` | 边界来自 `get_displays` 下发的工作区（面积并集），不是外接矩形 |
| 重力与反弹 | 飞行约 11.5 秒后静止 | 与 `gravity=1400 / restitution=0.78 / groundFriction=2.5` 的衰减过程一致 |
| 落地静止 | `y=1156`，工作区底 `1392`，宠物高 `236` | `1392-236 = 1156`，**精确踩在工作区底边上**（不陷进任务栏也不悬空） |
| 落地 Q 弹 | 冲击 `2173px/s` > `SQ_HARD_SPEED(1500)` | 触发最大幅度压扁 |
| 飞行中命中判定 | 飞行途中出现过"可交互↔点击穿透"翻转 | 那是真实系统光标恰好扫过宠物飞行路径（自测期间窗口在动、指针停着）；`input_busy` 的拖拽保护已在拖拽段验证（见下） |

**busy 保护的直接证据**（拖拽期间窗口**不得**翻回穿透）：

```
输入占用: 开始（Rust 侧停止兜底翻转）      ← 拖拽开始
…（拖拽全部 30 步期间没有任何"交互: 窗口切换为 点击穿透"）…
输入占用: 结束（恢复兜底翻转）              ← 拖拽结束
```

拖拽中指针位置与宠物包围盒相差 121px，若按几何盲判就会误判成"用户离开了"从而切断拖拽——
日志证明这段时间里一次都没有翻转。

---

## 6.1 逐帧窗口跟随（客观证据）

**方法**：两个独立证据源。

1. **前端计数**：每一次 `set_pet_bounds` 都会让 Rust 回传一次 `pet://window-state`，
   因此该计数 = 窗口**真的被移动了多少次**；
2. **外部采样**：用 `EnumWindows` + `GetWindowRect` 从进程外每 150ms 采一次窗口矩形，
   统计出现了多少个**不同位置**（160ms 的采样间隔远粗于窗口的移动频率，
   如果只是"结束时跳一下"，不同位置数会是个位数）。

**证据**：

```
自测: 窗口回传 总计=801 次，平均 63.3 次/秒（本次自测期间新增 800 次）
```

```
采样点数=110  不同位置数=77  尺寸集合: 840x656
轨迹抽样：130,130 → -114,763 → 592,94 → 1334,332 → 751,587 → 238,756 → 1483,874 → 1524,944 → 1231,946 → 1231,946
最终窗口: 1231,946,840x656
```

**判读**：

- **63.3 次/秒** ≈ 显示刷新率 → 窗口是**逐帧**被移动的（窗口移动由 `invoke` 驱动、
  Rust 端仅做 `set_position`，实测不构成瓶颈）；
- 外部采样出现 **77 个不同位置**、且尺寸**恒为 840×656**（运行期从不改尺寸，
  与"窗口尺寸在创建时确定"的设计一致）；
- 轨迹覆盖了拖拽（含 `x=-114`、`y=763` 等远离起点的位置）与飞行全过程，
  最后稳定停在 `1231,946`（对应包围盒 `(1441,1156)`，与前端日志完全一致）。

---

## 6.2 DPI 与缩放链路

**方法**：前端在启动时一次性记录 WebView 的缩放因子与窗口内布局度量，并与 Rust 侧的
物理像素尺寸对照。

**证据**：

```
缩放: devicePixelRatio=1 innerCSS=840x656 窗口CSS=840x656 包围盒CSS=420.0x236.0 命中区CSS=157.5x186.8
```

外部采样的窗口矩形：`840x656`（物理像素）。

**判读**——三处一致，证明 **1 逻辑像素 = 1 物理像素**（这正是 Rust 侧"按物理像素建窗、
坐标 1:1 传递"这一设计的前提）：

| 量 | 值 | 来源 |
| --- | --- | --- |
| 窗口物理尺寸 | `840×656` | `GetWindowRect`（外部进程） |
| 窗口 CSS 尺寸 | `840×656` | `window.innerWidth/Height` |
| `devicePixelRatio` | `1` | WebView |
| 包围盒 CSS 尺寸 | `420.0×236.0` | 配置 `size=420` + `9/16` 比例 |
| 命中区 CSS 尺寸 | `157.5×186.8` | `HIT_BOX` 等比换算 |

**结论**：宠物在屏幕上的实际像素尺寸与配置严格相等，缩放链路没有隐藏系数。
若将来在 125%/150% 缩放的屏幕上 DPR 不为 1，这条日志会立刻暴露（届时需要重定标
`coords.ts` 的换算收口，见 README 设计要点第 5 条）。

---

## 6.3 多显示器几何（工作区 vs 完整面板）

**方法**：Rust 启动时打印逐屏几何；前端记录收到的几何；启动流程内**强制校验**两者的内在一致性。

**证据**（本机单屏，但任务栏使两者不同，正好能验证区分是否正确）：

```
[whale-pet] 显示器：工作区 (0,0 2560×1392)；面板 (0,0 2560×1440)；主屏 (0,0 2560×1392)
显示器: 首帧几何 工作区 1 块 面板 1 块 主屏=2560x1392
```

**判读**：

- 工作区高 `1392`、面板高 `1440` → 任务栏占 48px，两者被正确区分（面板含任务栏）；
- 两者按序成对下发（长度相同），前端 `throwSpace({ areas, panels })` 分别用于
  「落地/反弹边界」与「越界侧有没有邻屏的探测」——这正是上游踩过的坑：
  上下叠放的屏在接缝处隔一条不属于任何工作区的条带，用工作区探测会让宠物永远穿不过去；
- 启动时执行 `display::validate_geometry()`：工作区必须被同序面板包含、长度必须相同、
  主屏尺寸必须为正，任一条不成立就**直接启动失败并报出原因**（而不是带着错误边界跑）。

**指纹轮询**：几何变化通过 1.5s 周期指纹比对检出（Tauri/tao 不提供显示器变化事件），
指纹同时覆盖工作区、面板与主屏三者——任务栏自动隐藏这类"只改面板"的变化也能检出
（已在冒烟检查中钉住）。


---

## 6.4 一个真实 bug 的定位与修复：首次拖拽"飞出去、控制不住"

**用户报告的现象**：启动后**首次**点击拖动会飞出去、鼠标按下状态"控制不住"，然后停住；
再点一次拖动就正常了。

### 定位过程（三次实测，逐步排除）

第一次尝试的结论（**错的，记录下来引以为戒**）：怀疑是"拖到命中区外松手导致 `pointerup` 丢失、
状态机卡在按下中"。为此加了按键看门狗（物理按键一松就强制收尾）与专项自测，
自测确实通过了——但用户反馈**现象依旧**，说明真正的根因还没找到。

第二次：加实时探针（`?autotest=3`）记录"采样坐标 / 窗口原点 / 命中判定"三元组。
把光标放到按窗口原点算出的宠物身体中心 `(2320,227)`，宠物立刻记
`命中: 首次进入身体`，移开后翻回穿透 → **几何与命中判定完全正确**，
`devicePixelRatio=1` 也排除了缩放问题。

第三次：在同一次按下里把三条坐标放在一起对照（光标用 `SetCursorPos` 停在"已知点"）：

```
按下: 来源=dom client=(420,336) →屏幕=(2320,226)   ← 命中判定用的坐标系
     采样=(1188,181)                                ← 宿主下发的全局光标采样
     真实光标位置=(1000,1200)                        ← 以为 SetCursorPos 设到了这里
```

当时据此判定 **`AppHandle::cursor_position()` 在本机返回的坐标不可信**。
**这个结论是错的**（后来复测推翻，见下面的"更正"）。
而早期实现恰恰**拿它去算抓取偏移与跟手目标**：

- 按下瞬间 `grabOffset = 采样坐标 − 包围盒` → 得到一个差 1000px 量级的垃圾偏移；
- 之后每帧目标位置 = 采样坐标 − 垃圾偏移 → 宠物被猛地甩开，表现就是"飞出去、控制不住"；
- 松手或环境变化后状态复位，第二次点击时采样值恰好接近真实位置，于是"第二次就正常了"。

### 更正（后来复测推翻了上面的结论）

`SetCursorPos` 在本机**根本停不住光标**：指针被桌面环境持续驱动，
设定 `(300,300)`、250ms 后读回 `(1844,627)`；连点三次的实测里，
目标 `(2320,226)` 每次点下去时光标分别在 `(2271,38)`、`(1991,173)`、`(2334,243)`。
所以上表里的"真实光标位置 `(1000,1200)`"**从来不是当时的真实位置**，
拿它当基准去比，自然会得出"采样不可信"的错误结论。

后来加了 `WHALE_PET_DIAG_CURSOR=1`（宿主把**它读到并广播出去**的值打进日志），
与页面侧 `?autotest=3` 的探针逐帧对照，结论完全相反：

```text
[whale-pet][光标采样] 宿主=(1922,635) 按键=true        ← 宿主 GetCursorPos 的读数
[d20714][pet-main-0] 探针: 采样=(1922,635) 包围盒=(2110,100) …   ← 页面收到的值
外部同刻 GetCursorPos = (1922,635)                     ← 另一个进程直接读
```

三条链路**逐像素一致**，而且与"宠物命中框内/外"的判定也完全吻合
（光标进身体 → `在命中框内=true` → `可交互=true`；移出 → 翻回穿透）。

**所以：采样坐标是准的**。跟手仍然用 DOM 坐标，但理由要写对——
DOM 坐标是"按下那一刻的真实位置"且**零延迟**，而采样通道要走
"宿主轮询 → IPC → 页面"三段，跟手会明显发飘。这是性能与精度上的选择，不是"采样在骗人"。

### 同一次排查里查出的**真 bug**：可交互状态两边不同步

采样坐标既然是准的，那"点击/穿透偶发失灵"就得另找原因。用 `WHALE_PET_DIAG_PRESS=menu`
跑全链路 + 进程外读窗口样式，找到一个真实的错位：

- **宿主**建窗时把窗口置为**可交互**（`PetWindowState.interactive = true`，注释写着
  "等前端完成首次命中判定后自然会翻成穿透"）；
- **前端**的"我已上报过什么"标志 `reportedInteractive` 从 `false` 起步。

于是前端第一帧算出的 `setInteractive(false)` 被自己的去重逻辑
（`if (interactive === reportedInteractive) return;`）吞掉了，宿主永远收不到那次翻转：
**窗口一直保持可交互**，包围盒里那些透明区域继续吃点击，直到用户第一次悬停宠物、
再移开才恢复。这正是 M0 那条"空白区域点不到下层页面"的老问题，在窗口压成包围盒之后
剩下的最后一条尾巴（包围盒比角色大，多出来的每一块都在吃点击）。

修法：把宿主的记账**下发**（`PetRuntimeDto::interactive`），前端在启动那次
`get_pet_runtime` 里对齐一次，之后才由本页的命中判定接管。实测启动日志：

```text
窗口: 初始 原点=(2110,100) … 宿主可交互=true     ← 前端对齐到宿主的真值
交互: 窗口切换为 点击穿透                         ← 34ms 之后
[whale-pet] 窗口 pet-main-0：可交互=false → ex=0x80C0138 transparent=true …
```

进程外探针复核：光标远在 `(1461,643)`（不在宠物上）时，宠物窗顶层就是 `T|L|NA` = 穿透 ✓
（修之前这里会一直停在"可交互"）。

### 修复

**坐标来源的第一原则：跟手/拖拽一律用 DOM 事件自带的坐标**
（`windowOrigin + event.clientX/Y`）——只有它天然与命中判定同一坐标系，而且它就是鼠标的真实位置。

- `pointerdown` 的抓取点改用 DOM 坐标；
- 跟手改由 `pointermove`（命中区 + window 两级）驱动，`pointerId` 校验防止多指针串台；
- `pointerup` / `pointercancel` 优先用 DOM；
- 全局光标采样通道**降级为两个职责**：① **按键看门狗**（收尾那些永远到不了页面的
  `pointerup`）；② **穿透期间的兜底位置**——且兜底起手只在窗口确实处于穿透态时允许，
  避免它跟 DOM 快路径抢同一次按下。

### 验证

注入式拖拽验证（宿主注入合成 `pointerdown` → 12 步 `pointermove`（每步 40ms）→ `pointerup`；
同时用 `WHALE_PET_AUTOTEST=4` 把按键位交给自测控制，让合成拖拽不被看门狗收尾）：

```
窗口: 初始 原点=(1900,-110) 尺寸=840x656 → 包围盒=(2110,100)
按下: 来源=dom client=(420,336) →屏幕=(2320,226) 窗口原点=(1900,-110) 包围盒=(2110,100)
拖拽: 开始（已打断飞行与 Q 弹）
拖拽: 结束                                   ← 531ms 后正常结束
松手: 甩出 速度=(-1993,417)px/s 位置=(1542,222)
```

**判读**：

- 抓取点 = DOM 推出的屏幕坐标 `(2320,226)`，与命中判定同一坐标系 →
  抓取偏移不再是垃圾值（这正是根因被消除的直接证据）；
- 整段拖拽期间**没有出现一次"由全局按键采样收尾"** → 在按键真实按下的情境下，
  看门狗（第 6.4 节上半部分引入的保护）不会误伤正常拖拽；
- 宠物从 `(2110,100)` 出发、按指针方向移动并带出松手初速（弹簧跟手 + 甩抛链路完好）。

### 后续：第二层根因——看门狗误杀（"飞出去一下再回来"）

用户复测后现象变成了"**形象会飞出去一下再回来**"，日志给出了直接证据：

```
19:39:16.972  按下: 来源=dom client=(429,331) →屏幕=(928,591)   ← 正常按下（此时宠物确在 (709,470)）
19:39:16.974  松手: 由全局按键采样收尾（DOM 事件丢失）           ← 2ms 后就被"收尾"
```

即：**第一轮为"松手丢失"引入的看门狗，本身就是新的误报源**。
本机的全局输入查询与光标坐标一样不可靠——`GetAsyncKeyState(VK_LBUTTON)` 会报"按键已松开"，
尽管用户正按着；按下瞬间看门狗立刻收尾，紧接着松手时的轨迹初速被当成甩抛
（`松手: 甩出` 或弹簧按错误目标甩一下），表现就是"飞出去一下再回来"。

**修复（三重条件 + 指针捕获）**：

1. **看门狗必须三条件同时成立**才收尾：① 按键位当前报"松开"；
   ② "报松开"的起始时刻**早于本次按下**；③ DOM 指针事件已静默 ≥ 250ms 且最后一次事件也早于本次按下。
   这样"按下瞬间按键位误报"不再触发收尾；
2. **指针捕获**（`setPointerCapture`）：拖拽期间把指针事件锁定在宠物窗口，
   光标滑出窗口边界也照常收到 `pointermove`——这是"拖拽中窗口翻回穿透、`pointerup` 丢失"
   那条老问题的正道解法，不再依赖任何全局输入查询；
3. `pointermove` 的 `pointerId` 校验、`pointercancel` 收尾、失焦收尾保持不变。

**验证**（让按键位在整段拖拽里都**误报"松开"**，即最坏情况；注入 12 步 × 40ms 合成拖拽）：

```
按下: 来源=dom client=(420,336) →屏幕=(2320,226) 窗口原点=(1900,-110) 包围盒=(2110,100)
拖拽: 开始（已打断飞行与 Q 弹）
拖拽: 结束                                   ← 511ms 后由指针抬起正常结束
松手: 甩出 速度=(-1921,408)px/s 位置=(1564,216)
飞行: 结束于 (2148,1038)，落地冲击 1555px/s
```

- 整段拖拽**没有出现一次"看门狗收尾"** → 误杀被消除；
- 抓取点仍是 DOM 屏幕坐标 `(2320,226)` → 第一层根因的修复保持有效；
- 该次日志里"飞行结束于 `y=1038`"而不是之前几次的 `1156`：本次记录在页面重载前被截断，
  看不到后续帧，**不能据此判定落点语义变了**。落点语义仍由纯逻辑决定
  （`throwStepRegion` 的 `atRest` 在 `y >= maxY-1` 且低速时成立，`maxY = 工作区底 − 宠物高 = 1156`），
  单屏落地应停在 `1156`；多次实测中的 `1156` 与之一致。

### 第三层根因：窗口会抢焦点 + 看门狗用了陈旧时刻

用户第三次反馈两个现象：**形象飞一下再回来**，以及**点击其他页面选中不了**。日志与样式探针给出两条独立原因：

**原因 A：宠物窗口抢焦点**（"点不了其他页面"的直接原因）

`focusable(true)` 时，鼠标在宠物身上按下会**激活该窗口并把它变成前台窗口**；拖拽结束、
窗口恢复点击穿透后，前台焦点仍留在宠物窗口上，用户去点别的窗口时第一次点击只被用来切换前台。
宠物被拖到屏幕中间时，那块 840×656 的透明置顶窗覆盖大片区域，症状尤其明显。

修复：窗口改为 **`.focusable(false)`**（鼠标按下按 `MA_NOACTIVATE` 处理，照常收到鼠标消息，
但永不激活）。并在启动与每次穿透翻转时打印窗口扩展样式，便于日后一眼判断：

```
窗口 pet-main-0：ex=0x8040118 transparent=false noactivate=true layered=false
窗口 pet-main-0：可交互=false → ex=0x80C0138 transparent=true noactivate=true layered=true
```

**实测断言**（真实鼠标 按下 → 分步移动 → 抬起）：

```
拖拽前前台 pid=2756 → 拖拽后=2756    （宠物进程 13816 从未成为前台窗口）
```

**原因 B：看门狗用了陈旧的 DOM 时刻**（"第一次拖拽异常"的直接原因）

看门狗判断"DOM 是否仍在投递事件"依赖 `lastDomPointerAt`，但 `pointerdown` 处理函数**忘了刷新它**。
于是第一次按下时读到的是上一次的陈旧时刻（实测 743ms 前），三重条件里的"DOM 已静默"被误判成立，
按下 **1ms 后**就被收尾：

```
按下: 来源=dom client=(420,337) →屏幕=(2050,1165) 包围盒=(1840,1038)
松手: 看门狗收尾（按键松开于 63221ms 前、DOM 静默 743ms，均早于本次按下）
```

修复：`pointerdown` 里先刷新 `lastDomPointerAt` 再判定。修复后的同一条真实拖拽：

```
按下: 来源=dom client=(420,337) →屏幕=(2050,1165) 包围盒=(1840,1038)
拖拽: 开始（已打断飞行与 Q 弹）        ← 不再被误收尾
拖拽: 结束                             ← 647ms 完整拖拽
松手: 甩出 速度=(-605,242)px/s 位置=(1591,1138)
```

**这两条也解释了"为什么第一次特别容易出问题"**：首次交互时 DOM 事件流刚建立、状态最陈旧，
而看门狗恰好用了那个陈旧时刻。

### 第四层根因：窗口外扩余量把"宠物周围一大片"变成了点击黑洞

用户第四次反馈：**形象周围很大一片空白区域都点不到下层页面**。

**源码依据**（`tao-0.35.3/src/platform_impl/windows/window_state.rs:285`）：

```rust
if self.contains(WindowFlags::IGNORE_CURSOR_EVENT) {
    style_ex |= WS_EX_TRANSPARENT | WS_EX_LAYERED;
}
```

即 Tauri 的"点击穿透"就是给窗口加 `WS_EX_TRANSPARENT`——这套机制本身是**正常工作**的
（实测：光标离开宠物身体后，窗口内角落的点击会穿透到下层进程）。

真正的问题是窗口**尺寸**：早期实现按 `WINDOW_MARGIN_RATIO = 0.5` 外扩，
即四周各多出半个宠物大小 —— **窗口 840×656，其中只有 420×236 是宠物**。
只要窗口处于"可交互"态（光标还停在窗口矩形内），整块矩形都在吃鼠标，
用户的实际感受就是"形象周围一大片空白都点不到下层"。

**修复**：把外扩比例改为 **0**（窗口尺寸 = 宠物包围盒尺寸）。实测：

```
窗口 pet-main-0：原点=(2110,100) 尺寸=420×236 包围盒=(2110,100)
缩放: devicePixelRatio=1 innerCSS=420x236 窗口CSS=420x236 包围盒CSS=420.0x236.0
```

窗口与宠物严格重合，UI 周围不再有任何空白区域。

**代价与后续**：气泡（余额/碎碎念）画在宠物上方、需要窗口外空间。
最终方案是**给气泡单独开一个不可聚焦的小窗**（已实现，见下文 ⑥）——
在那之前，"不挡用户操作"远比"预留气泡空间"重要。

**注意**：`WINDOW_MARGIN_RATIO` 是 Rust（造窗口）与前端（算坐标）之间的契约常量，
两边必须同时改——本次已同步 `pet_window.rs` 与 `coords.ts`。

## 6.5 M1：动画链 / 转向 / 漫游（实跑验证）

三项能力复用上游 `shared` 的纯逻辑（`pickers.ts` 的选择规则、`motion.ts` 的落点几何），
**没有重写一份**，验证结果如下。

### 动画链（播完自动接下一段）

素材：导入上游全部 **106 条**透明动画（51.9 MB）；动画池由
`node scripts/sync-animation-pool.mjs` 从上游 `config.jsonc` **结构化同步**（80 个分类动作名，
手抄必错）。启动时校验池内每个名字都能落到素材上：

```
[whale-pet] 动画池校验通过：106 条素材全部命中
```

连续运行 4 分钟的实测（每段约 10 秒 = 素材时长）：

```
动画链: 掷骰=action（当前 待机呼吸休闲，朝向 left） → 分类=小动作 动作=写代码
动画链: 掷骰=action（当前 写代码，朝向 left）        → 分类=吃什么 动作=吃糖葫芦
动画链: 掷骰=turn（当前 吃糖葫芦，朝向 left）        → 东张西望
动画链: 掷骰=idle（当前 东张西望，朝向 left）        → 待机呼吸休闲
动画链: 掷骰=action（当前 蝴蝶蜜蜂环绕头顶开花…）    → 分类=文字 动作=是啊，吃什么
动画链: 掷骰=action（当前 是啊，吃什么…）            → 分类=玩耍 动作=玩游戏气急败坏
```

24 次掷骰分布：`action 17 / turn 3 / idle 4`（未抽到 `move`，其权重只有 5%），
与 `animationWeights = idle 10 / turn 5 / move 5 + 分类 80` 的预期一致。
**`noMirror` 分类在朝左时正常参与抽取**（日志里出现过"分类=文字"）。

**踩到并修掉的一个坑**：待机最初按 `loop` 播放，而 `ended` 事件**只在非循环时触发**，
动画链因此从未被驱动（日志里只有启动、没有任何"掷骰"）。
现在**所有段一律按单次播**——"循环感"来自链反复抽到待机，与上游浏览器端做法一致；
另外加了一个看门狗定时器兜底（个别素材不触发 `ended` 时也能继续）。

### 转向与朝向翻转

`animations.turn` 的动画播完要翻转朝向。首轮实测 **3 次 turn 却没有一条"朝向"日志**——
原因是 `applyTurnFlipIfPending()` 写了但从未被调用（漏接线）。修好后：

```
[d20714 20:47:55.153][pet-main-0] 朝向: right（镜像）
```

镜像写在包裹宠物的元素上（`scaleX(-1)`），视频与命中区一起镜像；
命中框在水平方向居中，因此命中判定不需要额外的坐标翻转。

### 屏幕漫游

漫游权重只有 5%，自动化时等到它太慢，因此加了确定性入口
`WHALE_PET_AUTOTEST=5`（走的是与随机链**完全相同**的路径：`planMove` → `startWalk` → 逐帧推进）：

```
漫游: 首次行走 螃蟹走路 2320 → 2197 （2s 起 / 2s 止，时长 10.0s）
```

**窗口真的移动了**（进程外读窗口矩形）：

```
初始       窗口 = 2110,100
行走后     窗口 = 1987,100     ← 2197 − 210（外扩余量 0）= 1987，逐像素吻合
再行走一次 窗口 = 2018,100
```

`leadSec`/`tailSec` 期间原地不动、中间线性移动（`walkCenterAt`），
时长取自素材真实时长（`video.duration`），不是拍脑袋的常数。

---

### 右键级联菜单（⑤）

复用上游 `shared/menu.ts`（树 + 样式 + 级联渲染），桌面端只补"窗口从哪来"和"动作怎么落地"。

#### 架构：菜单是**独立小窗**（第二轮重构，取代了"临时扩大宠物窗"）

第一版把菜单画在宠物窗里，需要时把窗口临时外扩。那一套能跑，但代价是**一整套坐标换算**
以及随之而来的一串坑（这份文档里前后记了五条：外扩方向、偏移换算、重复右键偏移、
外扩期间要放宽命中区、换几何时人物虚影闪一下）。根子在于：窗口的移动/改尺寸由系统异步应用，
而"宠物画在窗口内什么位置"由页面 DOM 决定，**两者跨进程、不可能原子**。

现在改成与气泡同一套思路——**菜单自己一个窗**：

```text
[前端] 菜单: 右键触发 at 窗口内 (210,126) →屏幕 (2320,226)
[Rust] 菜单窗显示 menu-pet-main-0：右键点=(2320,226) →窗口原点=(1774,226) 内缩进=(546,0)
[菜单页] 菜单: 已弹出（窗口 780×520，内缩进 (546,0)）
```

- **摆位**由宿主做：`winX = clamp(clickX, 工作区)`、`insetX = clickX − winX`，
  两个值用同一个夹取结果算出来——**能放下时菜单正好在鼠标处，放不下时整体内移但不被屏幕裁掉**。
  实测右下角那个边界：宠物在 `(2110,100)` 点右键 → 窗口 `(1774,226)`、内缩进 `(546,0)` →
  菜单根面板屏幕坐标 `1774+546 = 2320`、`226+0 = 226` = 右键点 ✓；
  宠物在左下 `(24,938)` 时窗口夹到 `y=754`、内缩进 `(0,391)` → 面板仍在 `(24,1064)` = 右键点 ✓。
- **宠物窗从头到尾不改几何**：整个流程里（弹菜单、点菜单项、关菜单）宠物窗始终是
  `420×236`、「原点不变」。那一整类换算问题**结构性地消失**了，
  "换几何时人物虚影"也随之消失（再没有换几何这回事）。
- **动作落地**：菜单页只上报"用户点了什么"（`menu_action`），宿主转发成 `pet://menu-action`
  事件给宠物页执行——动画链、包围盒、气泡锚点都在宠物页手里，行为与旧实现完全一致。
  实测两条链路：

  ```text
  工具 → 显示一句气泡：菜单: 点播 anim=(无) action=say-demo → 菜单: 显示测试气泡 → 气泡窗显示
  工具 → 回到初始位置：菜单: 点播 anim=(无) action=home     → 菜单: 回到初始位置 (2110,100)（角落 top-right）
  ```

  > 顺带修掉一个**够不到的死代码**：`handleAction` 里一直有 `action === 'home'` 的分支，
  > 但上游 `buildMenuTree` 只产出"动画叶子"，永远不会产出 action——也就是"回到初始位置"
  > 这个菜单项**根本不存在**。现在把它真正挂进「工具」里，与路线图里写的功能对上。

- **什么时候关**：点了菜单项 / 在菜单矩形外**按下**鼠标 / 鼠标离开菜单 200ms（上游行为）/
  再次右键（直接换成新位置）。其中"外部按下"由**宿主**在光标轮询里判定：
  菜单窗是不可聚焦的（`focusable(false)`，点菜单不该顶掉用户正在用的窗口），
  所以它拿不到"失去焦点"事件；而光标位置与按键状态宿主本来就有。

- **代价（如实记录）**：菜单窗是 780×520 的**可交互**窗口（必须能收鼠标，否则点不到菜单项），
  因此菜单开着期间它覆盖的那块区域会吃掉点击。这与旧实现（外扩后的宠物窗 780×656）
  量级相同、且只持续到菜单关闭。关掉之后立刻恢复"只有宠物身体可点"。

#### 用户报的回归：用过"显示一句气泡"之后，再右键弹不出菜单

**现象**（用户实测）：右键 →「工具 → 显示一句气泡」→ 气泡出现又消失 → 再右键，菜单**不出现**。

**根因：页面与宿主互相调用对方的 close，形成无限回声。**

```text
宿主 close() → eval window.__whalePetMenu.close()   ← 让页面收 DOM
     ↑                                      ↓
     └── invoke('hide_menu') ← 页面 closeMenu() ─────┘
```

两边各自都很"合理"（宿主想让页面收 DOM、页面想让宿主藏窗口），合在一起就是每轮一次 IPC 的
死循环。实测把它抓得很干净——**一次菜单点播触发了 3427 次隐藏**：

```text
【两侧都还原成最初写法】菜单窗显示 = 11 次；已隐藏 = 3427 次；点播 = 1 次
```

窗口被这样一秒藏几千次，右键自然"弹不出交互窗口"（`show` 之后立刻被循环里的 `hide` 抹掉）。
**为什么自动化测试没抓到**：探针是往菜单窗里注入 DOM 点击的，DOM 点击不关心窗口是否可见——
只有用真实鼠标才会暴露。

**修法（两边都改，互为保险）**：

1. **宿主只负责窗口**：`menu_window::close` 只 `hide()`，不再 eval 页面去收 DOM。
   页面在下一次 `show()` 开头会顺手把上一份 DOM 收掉（见 `showMenu` 里的 `cleaningForShow`）。
2. **页面侧幂等**：`closeMenu()` 只在"确实挂着菜单"时才收 + 喊宿主；
   并且为"腾地方"这种关闭加了 `cleaningForShow` 抑制——否则刚弹出的菜单会被自己的
   `hide_menu` 立刻藏掉（同一类回声的另一面）。

**回归自检**：`WHALE_PET_DIAG_PRESS=menu` 现在把用户这条顺序原样跑一遍，并**自带断言**：

```text
01:40:28.008 [pet-main-0] 气泡: 自动隐藏                       ← 等满 6 秒
[whale-pet] 菜单窗显示 menu-pet-main-0：右键点=(290,1116) →窗口原点=(290,754) 内缩进=(0,362)
01:40:30.053 [pet-main-0] 菜单: 已弹出（窗口 780×520，内缩进 (0,362)）
[whale-pet][自检] 气泡消失后再右键：菜单窗仍可见 ✓               ← 1.5 秒后再查一次
```

同一次运行里"显示 3 次 / 隐藏 2 次（第三次还开着）/ 点播 3 次"——数量吻合，不再有任何回声。

#### 其它实测结论

- 命中范围**不再需要**"菜单打开时整窗放行"这个例外（菜单不在宠物窗里了）；
  宠物窗任何时候都只有身体命中区可交互；
- 上游菜单的 clamp 参数传的是整个菜单窗，于是多级面板永远落在窗口内、不会越出屏幕。
### 气泡独立小窗（⑥）

**为什么必须单独开窗**：宠物窗被刻意压成"严格等于宠物包围盒"（留透明余量会挡住下层点击，
用户实测反馈过），而气泡要画在宠物**上方**——只能另开一个窗。
这个窗必须是**不可聚焦 + 整窗点击穿透**的纯显示窗。

#### 坑一：在**同步命令**里建窗会死锁（这才是"气泡吃点击"的真正原因）

第一版气泡是"第一次 `say()` 时才创建"的，而 `show_bubble` 当时是**同步**命令——Tauri 的同步命令
跑在**主线程（事件循环线程）**上，`WebviewWindowBuilder::build()` 却要**阻塞等待 WebView2
控制器创建完成**，而那个完成回调必须由事件循环泵出来：等于自己等自己。

进程外探针 + 日志把症状钉死了：

```text
[whale-pet] 气泡窗开始创建 bubble-pet-main-0（锚点=(2320, 100)）   ← build() 之前打了这一行
（之后再没有任何气泡日志：没有"已创建"、没有页面加载事件、前端 invoke 的 Promise 永远挂着）
进程外窗口树：
  Tauri Window      (2172,14 296x86) T=False L=False
    WRY_WEBVIEW     (2172,14 296x86) T=False
      Chrome_WidgetWin_0 (2172,14 0x0)          ← WebView2 的渲染子窗**从未建出来**
```

三个症状叠在一起，正好就是用户感受到的"凭空多出一块 296×86 的死区，点不动下层"：
**窗口建出来了 + 内容一点没渲染 + 顶层没有穿透位**。

改法是一行：把 `show_bubble` / `hide_bubble` 声明成 `async`（命令改到异步运行时的工作线程上执行，
`build()` 走 Tauri 支持的那条"投递到主线程并等待"的正常路径）。改完日志立刻完整：

```text
[whale-pet] 气泡窗开始创建 bubble-pet-main-0（锚点=(2320, 100)）
[whale-pet] 气泡窗已创建 bubble-pet-main-0：原点=(2172,14) 尺寸=296×86 顶层NA(0x8040118) 子窗…
[whale-pet] 气泡页面加载事件 Started url=http://127.0.0.1:1420/bubble.html
[whale-pet] 气泡窗显示完成 bubble-pet-main-0：顶层T|L|NA(0x80c0138) 子窗T|L[…] T|L[…] T|L[…] T[…]
[whale-pet] 气泡页面加载事件 Finished url=http://127.0.0.1:1420/bubble.html
```

#### 坑二：`bubble.html` 没进生产构建

Vite 默认只把 `index.html` 当入口：`bubble.html` 在开发服务器下能访问、**生产构建里不会被产出**，
`tauri build` 出来的应用点开气泡只会得到 404 空窗——而"空窗"与坑一的症状一模一样，
所以这条也一并修了（`vite.config.ts` 的 `build.rollupOptions.input` 声明多页面入口）：

```text
dist/index.html                 4.12 kB
dist/bubble.html                （多页面入口后新增）
```

**④ 气泡没有出现在头顶上方（而是整体错到右下角）** —— 从菜单里点「显示一句气泡」必现。

原因：`show()` 原来让**宿主自己**用"当前包围盒原点"去推算"锚点相对包围盒的偏移"。
而菜单点播的顺序是 `close()（窗口缩回）→ onAction（say）`，中间那两次 IPC 会让宿主侧的
`box_offset` 与 `origin` 有**一小段时间互不匹配**（偏移已改回 0、原点还在外扩位置）。
宿主恰好在这段时间里读数，就得到"包围盒在宠物右下角 (360,420) 处"这个错值；
第一次显示用的是前端给的绝对锚点（正确），但只要宠物再动一下，`follow` 就会按错值重摆，
气泡整体偏移一个外扩量。

修法：**包围盒原点由前端随请求一起给**（`BubbleRequest::box_x/box_y`）——
前端手里永远是一份自洽的快照，交给它算偏移最稳。宿主不再自己去状态里读。

> **后续**：这条与上面的①②③一样，根因都是"菜单要临时改宠物窗几何"。
> 菜单改成独立小窗之后（见 ⑤ 的架构说明），"点菜单时宠物窗正在换几何"这个前提**不存在了**，
> 这一整类问题（含下面 ④⑤ 两条虚影/错位）随之消失。这里的记录保留，用于说明**当初为什么错**。

复现与验证用的是 `WHALE_PET_DIAG_PRESS=menu`：它把用户的操作顺序整条跑一遍
（注入右键 → 菜单打开 → 点「工具 → 显示一句气泡」→ 菜单关闭 → 气泡显示 → 再合成一次拖拽）：

```text
[前端] 菜单: 已打开（窗口 780×656，宠物偏移 (360,420)，菜单落点 (570,546)）
[前端] 菜单: 显示测试气泡
[前端] 气泡: 显示「…」锚点=(2320,131)
[Rust] 气泡窗显示 bubble-pet-main-0：308×100 页面 url=http://127.0.0.1:1420/bubble.html
进程外窗口：气泡 (2166,40) 308x100   宠物 (2110,100) 420x236
          尖角尖端 = 40 + 100 − 9 = 131 = 锚点 ✓
```

再让宠物被甩到屏幕另一头，气泡逐像素跟随：

```text
拖动后：宠物 (363,564)   气泡 (419,504)
期望：  363+210−308/2 = 419 ✓     564+31−100+9 = 504 ✓   （210/31 = 锚点相对包围盒的偏移）
```

**⑤ 气泡旁边有一块灰色虚影** —— 投影被窗口边界**裁成硬边**：气泡的 `box-shadow` 半径
24px，而窗口尺寸是"按文字估算的气泡本体大小"，四周一点余量都没留，
于是投影在窗口边界处被齐齐切断，看上去就是气泡旁边糊了一块灰。

修法：区分"**气泡本体**"与"**窗口**"两个尺寸——窗口在本体四周各留透明余量
（左右/上 14px、下 18px，其中 9px 给尖角），投影半径收小到能在余量内淡出。
Rust 侧 `bubble.rs::estimate` 的常量与页面 `bubble.html` 的 `body padding` **必须成对改**
（注释里互相点名，改一个不改另一个就会重新出现裁切）。
#### 坑三：`eval` 注入赶在页面导航之前（症状：日志全绿、屏幕空白）

气泡是"纯显示壳 + 宿主 `eval` 注入文本"，而 WebView2 的导航是**异步**的：
`build()` 返回时页面可能还停在 `about:blank`，此时脚本里的 `getElementById('bubble')`
拿到 `null`，文本被**静默丢掉**——窗口建好了、页面也加载了、样式位全对，屏幕上却什么都没有。
同一份代码两次运行的日志顺序不同（实测两种都出现过）：

```text
正常：气泡窗已创建 → 页面加载事件 Started → 气泡窗显示完成（注入）→ Finished
失败：气泡窗已创建 → 气泡窗显示完成（注入！）→ 页面加载事件 Started → Finished
```

修法：`show()` 注入前先把文本记进一张"待注入表"（`bubble.rs::PENDING_TEXT`），
`on_page_load(Finished)` 再补注一次——两种时序都能落地；隐藏时清空该表，
避免页面（热重载等原因）重载后凭空冒出一句已经过期的旧话。
同时在 `show()` 里把**注入当时的页面 url** 打进日志：

```text
[whale-pet] 气泡窗注入 bubble-pet-main-0：页面 url=about:blank        ← "赶早了"的铁证
[whale-pet] 气泡窗注入 bubble-pet-main-0：页面 url=http://127.0.0.1:1420/bubble.html
```

修好后两种时序都实测过：新窗口（`about:blank` → 靠 `Finished` 补注）与复用窗口（页面已在，
直接注入）都能正常显示，截图分别为"气泡独立小窗自测…"与"气泡复用自测：第二句…"。
#### 体验修正（用户反馈三条，逐条改）

**① 首次出现很慢** —— 原来都是"第一次 `say()` 才建窗"：WebView2 建窗 + 页面加载要几百毫秒，
点完菜单得愣一下。改成**启动时就把气泡窗建好并隐藏**（`bubble::prepare`，紧随宠物窗之后），
显示时只改尺寸/位置。日志上很直观：

```text
启动阶段：[whale-pet] 气泡窗已创建 bubble-pet-main-0：120×60 顶层NA(0x8040118) …
启动阶段：[whale-pet] 气泡页面加载事件 Started/Finished url=http://127.0.0.1:1420/bubble.html
显示时刻：[whale-pet] 气泡窗显示 bubble-pet-main-0：296×86 页面 url=http://127.0.0.1:1420/bubble.html
```

显示那一刻页面**已经是 bubble.html**（不是 `about:blank`），没有建窗、没有导航等待。
代价是一只预先存在的 WebView2 实例（几十 MB）与启动时多出的建窗时间；换来"点了就出现"。

**② 气泡离人物太远、没有箭头** —— 两个原因叠在一起：

- 锚点原本就是包围盒**顶边**，而包围盒是整段视频的外框，角色头顶在框内还空着几十像素；
- 尖角用 `#bubble::after` 画在气泡**外面**（`bottom:-9px`），而窗口高度没给这 9px 留位置，
  **尖角被窗口裁掉了**——所以用户看到的是"一个没有箭头的白方块飘在天上"。

修法：页面 `body` 补 `padding-bottom: 10px` 给尖角留位置；锚点下压
`BUBBLE_HEAD_RATIO = 0.13 × 包围盒高度`（≈31px，见 `runtime.ts` 的常量注释），
尖角正好戳在头发上。顺带把气泡样式做细了一点（圆角 16、1px 描边、更柔的投影、
淡入时带一点位移+缩放）。

> 踩过的坑：第一次把系数乘在了包围盒**宽度**上（420×0.13≈55px），气泡直接盖住头顶。
> 现在乘的是高度（`size × aspectRatio`）。

**③ 气泡不跟随宠物** —— 原来只在 `show()` 时摆一次位置，拖动/抛掷/漫游之后气泡留在原地。

修法：`show()` 把锚点换算成"**相对包围盒原点的偏移**"记进宿主侧记账表；
`set_pet_bounds`（宠物窗移动的唯一出口）之后顺手调 `bubble::follow` 重摆气泡。
放宿主侧而不是前端，是为了省掉每帧一条 IPC；前端只负责算一次头距，两边不会各算一套。

实测（真鼠标拖拽 ≈ -187,+140）：

```text
拖动前：宠物 (2110,100) 420x236    气泡 (2172,69) 296x86
拖动后：宠物 (1923,240) 420x236    气泡 (1985,209) 296x86
        位移 (-187,+140)            位移 (-187,+140)   ← 完全一致
```
#### 验证证据（全部进程外）

**① 窗口样式位**（`scripts/probe-window-styles.ps1`：外部进程读 `GWL_EXSTYLE`）：

| 窗口 | 扩展样式 |
| --- | --- |
| 气泡顶层 `Tauri Window` (2172,14 296×86) | `T\|L\|NA` = `0x80C0138` |
| `WRY_WEBVIEW` / `Chrome_WidgetWin_0` / `Chrome_WidgetWin_1` / `Chrome_RenderWidgetHostHWND` | **全部带 `T`** |
| 宠物顶层（交互态，对照） | `NA` = `0x8040118`（没有 `T`） |

`WS_EX_TRANSPARENT`（T）= 命中测试时跳过本窗口；`WS_EX_LAYERED`（L）= 分层窗。
这一对位正是 tao 实现"点击穿透"用的那一对（`tao-0.35.3/src/platform_impl/windows/window_state.rs:286`），
宠物窗的穿透走的是同一条路径。加守护线程后在气泡可见期间反复采样，**始终**是这个值。

**② 视觉**：屏幕截屏（`CopyFromScreen` 抓 (2130,0) 400×160）第一次证明了气泡**真的画出来了**——
白底圆角气泡 + 尖角指向宠物头顶，文本渲染正确（见下面的复用截图）。

**③ 真鼠标点击的 A/B 对穿实验**（`scripts/test-bubble-click-through.ps1`）：
把气泡**故意压在宠物身上**（`?autotest=8`），然后在两个点上各打一次**真鼠标点击**
（`SetCursorPos` + `mouse_event`，走系统输入队列，不是合成 DOM 事件）：

| 探针点 | 坐标 | 覆盖关系 | 宠物日志里的结果 |
| --- | --- | --- | --- |
| 重叠点 | (2320,270) | 气泡**和**宠物都覆盖 | `按下: 来源=dom 指针=(2320,270)` → 播放"点击回应" |
| 对照点 | (2320,140) | 只有宠物覆盖 | `按下: 来源=dom 指针=(2320,140)` → 播放"点击回应" |

脚本还会自校验前置条件（从日志里解析气泡窗矩形 `2172,224 296×86`，确认重叠点确实落在泡里）。
**结论：点击穿过了气泡落到宠物身上——气泡不吃点击。**

> 判据为什么按坐标匹配、而不是按"有没有切到点击回应动画"匹配：动画文件名在日志里是
> **百分号编码**的（`%E7%82%B9%E5%87%BB...`），用中文关键字匹配会**永远**不命中（踩过）。

**④ 隐藏后复用**（`?autotest=9`：显示 → 3 秒后自动隐藏 → 再显示）：
`气泡窗已创建` 全程只出现 **1 次**，`气泡窗显示完成` 出现 **2 次**（第二次是复用同一个窗口），
第二次显示期间进程外读到的样式仍是 `T|L|NA`，截图里文本已换成第二句
（"气泡复用自测：第二句，验证同一个窗口被复用"）——隐藏/显示动作**没有**把穿透位抹掉，
页面也没有被重新加载（没有第二组 `页面加载事件`）。

**⑤ 不抢焦点**：`noactivate=true`（与宠物窗同一套 `focusable(false)`），
自动隐藏默认 6 秒（`say()` 会重置计时器，不会出现旧计时器提前关掉新气泡）。

#### 关于"第二轮那三个探针互相矛盾"的更正

第二轮用 `WindowFromPoint` 的"对照法"得出过"气泡在吃点击"的结论，**那个结论是错的**：
`WindowFromPoint` 的语义是"返回包含该点的窗口"，它**不跳过**带 `WS_EX_TRANSPARENT` 的窗口
（本轮实测：宠物中心点命中的就是带 `T` 的 `Chrome_RenderWidgetHostHWND`）。
真正的判据只有两个：**进程外读样式位**（机制）与**真点击看谁收到**（行为）——都在上面了。

---

### 视觉验证：宠物与气泡同框（本轮第一次做到）

M0 一直把"宠物到底看不看得见"列在"未能验证"里，理由是 `PrintWindow` 抓不到 WebView2 的合成内容。
**换个 API 就解决了**：`CopyFromScreen`（抓屏幕 DC，PowerShell + `System.Drawing`）拿到的是
DWM 合成之后的真实画面，WebView2 的内容照样在里面。

`WHALE_PET_AUTOTEST=6`（气泡自动显示 5 秒）时抓 `(2090,0) 460×380`，一次拿到三件事：

- **宠物本体清晰可见，且背景真正透明**——能直接看到桌面与其它窗口的标题栏穿过她周围；
- 气泡画在**头顶正中**，白底圆角 + 向下的尖角指向头顶，与设计一致；
- 气泡窗与宠物窗**不重叠**，这正是 ⑥ 里"点击归属"要额外设计 A/B 实验的原因。

截图落在构建产物目录（不入库）：`src-tauri/target/pet-and-bubble.png`。

---

### 保留下来的排障设施

这次定位靠的是"可控输入 + 可读日志"，因此把它们固化下来（全部由**环境变量**开关控制，
默认不启用、不参与运行路径，见 `src-tauri/capabilities/default.json` 的说明）：

| 开关 | 作用 |
| --- | --- |
| `WHALE_PET_AUTOTEST=1` | 页面侧受控拖拽 + 甩抛自测（弹簧滞后/初速/落地） |
| `WHALE_PET_AUTOTEST=2` | "拖到命中区外松手"场景（看门狗收尾验证） |
| `WHALE_PET_AUTOTEST=3` | 实时探针：采样坐标 / 窗口原点 / 命中判定三元组 |
| `WHALE_PET_AUTOTEST=4` | 把按键位交给自测控制（供注入式拖拽验证） |
| `WHALE_PET_AUTOTEST=5` | 强制走一段（漫游权重只有 5%，自动化时等到它太慢） |
| `WHALE_PET_AUTOTEST=6` | 显示一句气泡（5 秒后自动隐藏） |
| `WHALE_PET_AUTOTEST=7` | 同 6，但气泡保持 60 秒——只为给进程外探针留采样时间 |
| `WHALE_PET_AUTOTEST=8` | 把气泡**压在宠物身上**，供"真鼠标点击"判定点击归属（A/B 对照） |
| `WHALE_PET_AUTOTEST=9` | 气泡"显示 → 隐藏 → 再显示"，验证窗口复用与穿透位保持 |
| `WHALE_PET_DIAG_PRESS=1` | 注入一次合成 `pointerdown`（两条坐标链对照） |
| `WHALE_PET_DIAG_PRESS=drag` | 注入一次合成拖拽（12 步 × 40ms，验证跟手） |
| `WHALE_PET_DIAG_PRESS=right` | 注入一次合成右键（验证右键菜单链路与外扩几何，见 ⑤） |
| `WHALE_PET_DIAG_PRESS=menu` | 右键 → 在**菜单窗**里点「工具 → 显示一句气泡」→ 合成一次拖拽 → **等气泡自动消失后再右键一次并自检菜单窗是否仍可见** → 再点「回到初始位置」（完整复现用户操作顺序，含那条回归） |
| `WHALE_PET_DIAG_CURSOR=1` | 每 ~0.5 秒打印宿主读到的光标值与按键位（与页面 `?autotest=3` 的探针对照，判定"采样是否可信"） |

进程外的两个诊断脚本（**不在应用运行路径里**，只在排查时手工跑）：

| 脚本 | 作用 |
| --- | --- |
| `scripts/probe-window-styles.ps1` | 按节奏采样本进程全部顶层窗口的类名/矩形/扩展样式位（含子窗口树） |
| `scripts/test-bubble-click-through.ps1` | 真鼠标点击的 A/B 对穿实验（重叠点 vs 对照点），带前置条件自校验 |

---

## 6.6 手感调整：甩出太灵敏 + 停下后抓不到（用户真机反馈）

### 先把"是不是算错了"排掉

第一条反应是怀疑初速估算（`shared/physics.ts` 的 `estimateReleaseVelocity`）在虚高。
结果发现**是测试工具在骗人**：合成拖拽注入的是**固定的 client 坐标**，
而 `clientX` 是相对窗口的——拖拽时宠物窗跟着指针移动，于是"固定 client 坐标"在屏幕上
是个**随窗口漂移的点**；页面算出的屏幕点 = `窗口原点 + clientX`，
轨迹里就混进了"窗口自己移动的那一段"，初速被整体放大
（实测：匀速 340px/s 的合成拖拽被估成 1214px/s）。

修好注入器（改成注入**屏幕坐标**、每步现算 `clientX = 目标屏幕 x − window.screenX`）之后：

```text
位移 (-300,+64) 分 12 步 × 40ms = 480ms   → 指针速度 ≈ 639px/s
松手: 甩出 速度=(-520,111)px/s             → 估算 532px/s ≈ 指针速度 ✓
```

**估算本身是准的**。也就是说"过于灵敏"不是数学问题，而是上游那套参数的手感取向：
死区 `DEAD_ZONE_SPEED = 500px/s` 偏低、峰值加权 50%、末段加速还能再放 60%——
按"甩得很凶"调的，随手一抛就飞很远，加上重力 1400 与弹性 0.78，飞行+弹跳要好几秒。

### 改了两处（都在我们自己的层里，`reference/shared` 保持零改动）

1. **甩出速度整体打折** `RELEASE_SPEED_SCALE = 0.6`（`runtime.ts`）：
   等价于把手感从"轻甩即飞"拉回"用力才飞"，同时保留"越用力飞越远"的单调性。
   实测同一手势：估算 532px/s → 实际甩出 **319px/s**（原来是 532px/s 直接飞出去）。
2. **松手落点夹回工作区**（`clampBoxToWorkArea`）：
   拖拽**不限制越界**（可以把宠物拎到屏幕上方去），而"温柔放下"是按松手位置直接落位——
   于是宠物可能停在屏幕外/半挂边缘，**用户再也抓不到它**（用户说的"停止后就无法操控"）。
   夹取规则与抛掷边界一致：左右各允许越出 15%，垂直方向必须完整在屏内。

```text
松手: 落点在屏外，夹回工作区 (2160,-402) → (2160,0)     ← 修之前会停在 y=-402
松手: 温柔放下 位置=(2160,0)
```

顺带确认"甩出去之后还能不能操控"：同一次运行里的第二次拖拽（从落地位置起手）正常生效：

```text
按下: 来源=dom 指针=(1048,860) 包围盒=(838,734)   ← 宠物被甩到 (838,734) 后仍可按下
拖拽: 结束 → 松手: 温柔放下 位置=(888,232)
```

### 调这个手感

`src/renderer/runtime.ts` 顶部的 `RELEASE_SPEED_SCALE` 是唯一的旋钮（1.0 = 上游原样，
0.6 = 当前）。要更钝继续往下调即可。如果希望做成配置项（像 `physics` 那样可热改），
可以再加一段 `feel`，但那要动配置结构，等你确认需要再做。

---
### 6.6.1 更根本的一条：兜底通道与前端"互相打架"（用户第二次反馈）

上面两条改完之后用户仍然报"还是存在类似的问题"，并贴了日志。日志里有一段很好认的模式：

```text
[whale-pet] 可交互=false → transparent=true      ← 前端要求穿透
[whale-pet] 可交互=true  → transparent=false     ← 兜底立刻翻回可交互（**前端没有对应日志**）
[whale-pet] 可交互=false → transparent=true
[whale-pet] 可交互=true  → transparent=false
…
```

**"只有一条边有日志"就是打架的铁证**：前端每次翻转都会打 `交互: 窗口切换为 …`，
而上面这些 `可交互=true` 前面都没有它——说明是**兜底通道**（Rust 的光标轮询）单方面翻的。

根因是**两边用了不同的命中几何**：

| 判定方 | 用的矩形 | 位置 |
| --- | --- | --- |
| 前端（精确判定） | **身体命中区** 158×187（`hitbox.ts::scaleHitBox`） | 命中区之外一律要求穿透 |
| 兜底通道（Rust） | **整个包围盒** 420×236（`box_rect()`） | 圈内就翻回可交互 |

包围盒里"命中区之外的那一圈"是**透明像素**（宠物周围的空白），前端判定应当穿透，
兜底却坚持翻回可交互——于是每一帧：前端翻穿透 → 兜底翻回来 → 前端再翻穿透 → …
实测表现为**那一圈时而吃点击时而不吃**：用户点了没反应、抓不住宠物
（用户两次反馈的"无法操控"都是它）。

修法：`apply_fallback_hit` 改用**与前端同一套命中几何**（`box_origin + config.hit_box`，
两边都由 `CANVAS_HIT_BOX` 等比例换算而来，Rust 与 TS 各有一份同源实现）。
两边在同一时刻必然得出同一结论，**结构上不可能再打架**。

实测（`WHALE_PET_DIAG_PRESS=drag`，一次抛掷 + 一次温柔放下）：

```text
【修复后】Rust 翻可交互=0 翻穿透=1 ；前端翻可交互=0 翻穿透=1     ← 完全一致
飞行全程没有任何翻转（光标不在身体上 → 安静地保持穿透）
```

修之前同一条路径下，每次抛掷/落地都会出现上面那种成对翻转。

---
## 6.7 一次真实死锁：在持锁期间跨线程改窗口样式（用户第三次反馈"现在会卡死了"）

> 这是本项目到目前为止最难定位的一个 bug。前两次（6.6 / 6.6.1）我都在**症状层**打转，
> 这次靠**进程外证据**才挖到根因。结论先写在最前面：
> **光标轮询线程持有 `state.pets` 锁去改窗口样式，而那个窗口归主线程所有。**

### 现象（用户现场日志，末尾就是断点）

```text
[pet-main-0] 松手: 甩出 速度=(-2918,-185)px/s →折扣后 (-1751,-111)px/s 位置=(1523,738)
[pet-main-0] 飞行: 开始，边界 1 块屏
[pet-main-0] 交互: 窗口切换为 点击穿透
[whale-pet] 窗口 pet-main-0：可交互=false → ex=0x80C0138 transparent=true noactivate=true layered=true
```

之后**再也不出任何一行日志**，宠物停在空中、整窗透明、点不动。第一反应容易以为是
"又有窗口在吃点击"或"渲染卡住了"，但两者都不是（视频也停在那一帧）。

### 进程外证据（四条，互相印证）

| 探针 | 结果 | 说明 |
| --- | --- | --- |
| `IsHungAppWindow` + `SendMessageTimeout(WM_NULL)` 打三个顶层窗口 | 全部 `HUNG`（2s 无响应） | 三个 Tauri 窗口都归**主线程**所有 |
| 同一探针打 WebView2 的子窗口（`Chrome_RenderWidgetHostHWND` / `Intermediate D3D Window`） | 全部 `ok`（秒回） | WebView2 侧线程**没问题** → 不是跨进程互等 |
| `Get-Process` CPU 采样 6 秒 | 应用 `+0.00s`，渲染进程 `+0.19s` | 主线程在**等**而不是死循环；页面还活着 |
| 窗口矩形截图（`CopyFromScreen`） | 只有壁纸，宠物不见 | 窗口在 (847,1033)，但内容已经不再绘制 |

### 根因：两个线程互等

```text
光标轮询线程                                  主线程（事件循环）
────────────────────────────────────────     ─────────────────────────────────
state.pets.lock()   ← 拿到锁
  runtime.apply_interactive(true)
    → window.set_ignore_cursor_events(..)
      → SetWindowLongW(hwnd, GWL_EXSTYLE)     ← 该窗口归主线程所有
         Windows 会 SendMessage(WM_STYLECHANGING)
         给主线程并**等它派发**（阻塞在这里）
                                              set_pet_bounds / pet_debug_log …
                                                state.pets.lock()  ← 等锁（被轮询线程持有）
                                                于是回不到消息循环
                                                → 上面那条消息永远派发不出去
        ××× 双方永久互等 ×××
```

- 违规点只有一处：`lib.rs::apply_fallback_hit` —— **在锁内**做了窗口操作。
  项目自己的纪律（`state.rs` 顶部）早已写着"绝不在持锁期间做 IO 或跨 webview 调用"，
  这条路径是唯一的例外，而且它跑在**非主线程**上。
- 为什么"时而卡、时而好"：死锁要求**恰好**在"轮询线程持锁做窗口调用"的那几百微秒里，
  主线程正好在等这把锁。拖拽/抛掷期间前端以 60 次/秒发命令（`set_pet_bounds` +
  每条日志一次 `pet_debug_log`），命中概率随动作剧烈程度上升——
  这与用户的观感"甩得越猛越容易卡"一致。
- 为什么上一次修复（积分窗穿透）没能解决它：那是**另一个真 bug**
  （"`show()` 之后必须补回穿透位"），与这条互等无关。用户回滚积分后仍然卡，正是这条在起作用。

### 修复（两段式 + 投递主线程）

1. `PetRuntime::plan_interactive`：**持锁期只改"意图账本"**（返回是否需要翻转），绝不动窗口；
2. `pet_window::apply_interactive_style`：只凭窗口句柄落样式，供"出锁期 / 主线程"调用；
3. `lib.rs::apply_fallback_hit` 拆成两段：① 持锁只做判定 + 记账，产出标签清单；
   ② 出锁后用 `AppHandle::run_on_main_thread` 把"落样式"**投递给主线程**（非阻塞投递）。
   于是轮询线程既不在持锁时动窗口，也不会阻塞在任何窗口调用上——**死锁环从结构上不成立**。
4. `with_pet`（前端命令那条路径）与 `broadcast_cursor` / 菜单外点关闭 / 气泡跟随取锁，
   统一改用带计时的 `watchdog::timed_lock`，让"锁被长期持有"以后一眼可见。

### 新增看门狗（`src-tauri/src/watchdog.rs`）

| 设施 | 阈值 | 作用 |
| --- | --- | --- |
| `timed_lock`（等锁计时） | 300ms 告警 / 3s 疑似死锁 | 把"锁被长期持有"变成日志里看得见的事实 |
| `timed`（窗口操作计时） | 200ms | 窗口/跨 webview 调用是唯一会"阻塞在别的线程上"的东西 |
| `MainThreadWatch`（主线程泵动） | 每秒打点，3s 无泵动告警 | **独立线程**——关键点：它不能和"嫌疑人"共用一条命 |

### 验证

**A/B 对照**：临时注入 `WHALE_PET_DIAG_LOCKSTALL=<ms>`（在 `apply_fallback_hit` 的持锁段里
人为 sleep，把竞态窗口放大到必中），用同一段真鼠标动作（光标移到宠物身上）触发：

| 组 | 代码 | 注入 | 结果 |
| --- | --- | --- | --- |
| A | 修复前（`7ee6e35`） | 250ms | **必卡死**：三个顶层窗口全部 `pump=HUNG` / `IsHungAppWindow=True`，日志停在最后一次"交互: 窗口切换为 …"翻转（与用户现场同一形态） |
| B | 修复后 | 250ms | 不卡：`pump=ok` / `IsHungAppWindow=False`，交互链路照常（穿透 → 可交互 → 点击回应） |
| B′ | 修复后 | 4000ms | 不卡，且看门狗按设计发声：`等锁 pets（前端命令）用了 ~4000ms（≥3000ms，疑似死锁…）`（连续 6 次） |

**看门狗自身的验证（进程外、零代码改动）**：用 `SuspendThread` 把主线程冻住 6 秒 →
`[whale-pet][看门] 主线程已 3015ms 没有处理任何事件（疑似死锁/长阻塞）…` ✓，恢复后应用照常。

**功能回归（真鼠标，进程外）**：悬停（就是上表的复现手法）→ 拖拽 → 松手甩出 →
**在空中再抓住** → 落地静止 → 连续 4 次真点击：`dom 按下次数 = 12`、`点击回应次数 = 4`，
全程 `pump=ok`，且**没有任何看门狗告警**（阈值不误报）。
菜单/气泡自检同样通过：`[whale-pet][自检] 气泡消失后再右键：菜单窗仍可见 ✓`。

### 教训（写给下一次）

- 症状层修三次不如一次拿**进程外证据**：`IsHungAppWindow` 一句话就把范围从
  "渲染 / 穿透 / 物理 / 窗口样式"缩小到"主线程被阻塞"。
- "谁在等谁"必须两边一起看：只看前端日志会以为"窗口卡在点击穿透"，只看 Rust 日志
  会以为"前端没发命令"。
- 看门狗必须跑在**不会被卡住的那个线程**上：第一版挂在轮询线程里，而真实死锁恰恰卡住的
  就是轮询线程——等于没有看门狗。

---

## 6.8 两个输入状态机的 bug：只有首次能甩出 + 穿透态按下被吞掉（用户第四次反馈）

用户反馈两句话：

1. "拖动甩出不太丝滑"；
2. "另外只有首次可以拖动甩出，后续拖动就不能甩出了"。

第 2 条是硬 bug，查下去又牵出第 1 条的一个真原因。

### 6.8.1 "只有首次能甩出"：飞行标志漏复位 + 守卫（真 bug）

- `ThrowAnimator.stop()` 按设计**不回调 `onRest`**（"被抓住"不算"落地"），
  而运行时的 `flying` 布尔量**只在 `onRest` 里复位** → 空中被抓住一次，这个标志就永远是 `true`；
- `startThrow` 开头有 `if (this.flying) return;` → 之后每一次松手都被**静默**跳过：
  日志里能看到 `松手: 甩出 速度=…`，但**没有** `飞行: 开始`。
- 复现（临时把老写法搬回来）：新增的自测 `?autotest=10` 报 **起飞 1/6**（只有第一次飞），
  与用户描述逐字一致。

**修复**：

- `flying` 改成**只读属性**，直接读 `ThrowAnimator.running`（唯一真相，结构上不可能不同步）；
- 删掉 `startThrow` 的守卫（`ThrowAnimator.start()` 内部本来就先 `stop()`，重复调用语义正确）；
- 新增 `interruptFlight()` 收口"打断飞行"（停 rAF + 取消 Q 弹），`beginPress` 与拖拽起手共用。

### 6.8.2 穿透态下"按下"被整个吞掉：兜底起手是死代码（真 bug）

`evaluateInput()` 里的原写法：

```ts
void this.setInteractive(insideBody);                          // ← 同步改写 reportedInteractive
if (primaryDown && insideBody && !this.reportedInteractive) {   // ← 于是这个条件恒为假
  this.beginPress(cursor, 'sampler');
}
```

`setInteractive()` 是同步赋值的，下一行读到的必然是自己刚设好的值——
这个分支**在任何情况下都不会命中**：窗口处于穿透态时（DOM 事件到不了页面）的按下
没有任何人处理，用户感受就是"点了没反应，得再点一次"，而 `?autotest=1/2` 这些合成自测
也因此全线失效（自测日志里全是"身体内按下未被识别"）。

**修复**：先取 `const wasInteractive = this.reportedInteractive;` 再调 `setInteractive`，
判据改成 `!wasInteractive`（"这一帧**开始时**窗口还是穿透态"才是采样兜底起手的条件）。
可交互时的按下仍走 DOM——那是零延迟、坐标最准的那条路，DOM 侧本来也有 `drag.isPressed` 去重。

### 6.8.3 新增自测钩子 `?autotest=10`（回归网）

`WHALE_PET_AUTOTEST=10` → 每轮"甩出去 → 150ms 后在空中抓住 → 再甩一次"，3 轮共 6 次起飞，
**每次都必须真的起飞**。合成输入走运行时自己的状态机（不需要真鼠标：本机上
`SetCursorPos` / `mouse_event` 会被桌面环境持续挪走，实测不可靠）。

```text
修复后：      重复甩出自测: 结果: 起飞 6/6 次 —— ✓ 每轮都成功起飞（重复甩出可用）
（临时模拟老写法）重复甩出自测: 结果: 起飞 1/6 次 —— ✗ 有甩出没有起飞（回归！）
```

顺带采到的手感数据（修复后）：

- 拖动跟随（9px/16ms ≈ 560px/s 的拖动）：弹簧滞后 **平均 41~55px、峰值 57~75px**
  —— 这是上游弹簧（K=200 / C=30）的固有滞后，不是卡顿；
- 飞行：帧步长 **平均 12~15px、峰值 46~65px**，142~234 帧**每帧都有位移**（无停顿）。

### 6.8.4 跟手手感：把随动滞后从 0.15s 压到 0.082s（用户定性反馈后定案）

用户选中了"拖动时宠物跟得太松/飘（滞后明显）"这一项。查下来的结论很干净：

- 跟手是上游的过阻尼弹簧（`SPRING_K = 200` / `SPRING_C = 30`），其**稳态滞后**为
  `v · C / K`——**与刚度无关，只由 C/K 决定**，且 `C/K = 0.15s`：
  等价于"宠物永远比光标晚 0.15 秒"，1000px/s 的拖动就落后 150px。
- 共享层的 `springStep` 带一个 `power` 参数（K/C 同乘），但它只改变**收敛速度**、
  不改变稳态滞后——所以配置里的 `throwPower` 调不出"更跟手"。

**改法（唯一一处偏离上游的物理常量，`reference/shared` 保持零改动）**：
在 `src/renderer/drag.ts` 里用同一套公式、自己的 K/C：
`K = 600`，`C = 2√K ≈ 49`（临界阻尼 ζ=1，不 overshoot）→ `C/K = 0.082s`（原 55%）。
显式欧拉的稳定步长上限 `2/√K ≈ 82ms` 仍远大于 `MAX_SPRING_DT = 50ms`（留了余量，
再往上翻倍就要提防卡顿后的抖动）。旋钮就是 `DRAG_FOLLOW_K` 这一个常量。

**验证（`?autotest=10` 的滞后采样，同一段 9px/16ms ≈ 560px/s 的合成拖动）**：

| | 平均滞后 | 峰值滞后 | 起飞 |
| --- | --- | --- | --- |
| 调之前（上游 K/C） | 41~55px | 57~75px | 6/6 |
| 调之后（K=600） | **23~29px** | **31~41px** | 6/6 |

飞行的帧步长不变（平均 13~17px、峰值 43~65px，每帧都有位移），说明只改了"跟手贴不贴"，
没有动抛掷物理。质量门同样全绿。

**用户验收（2026-09-19，真机）**："可以，当前版本很丝滑。" —— 本轮三个提交
（`4c17fef` 死锁、`510fa7d` 输入状态机两处、`d08286a` 跟手弹簧）合并后的版本通过验收。
至此 M1 的输入/窗口链路不再有已知的卡死、吞按下、甩不出去这三类问题。

---
## 7. 纯逻辑冒烟检查

**方法**：`cargo run --bin logic-smoke`（39 项断言）。

**证据**：通过 **39 项**、失败 0 项，退出码 `0`。覆盖：

- 默认配置模板可解析并通过校验、尺寸与物理参数与上游默认值一致；
- 配置校验的四条拒绝路径（空 `pets`、非法 `id`、过小 `size`、未知 `schemaVersion`）；
- 物理参数取值范围的三条拒绝路径（恢复系数 >1、负重力、力度 0）；
- `Rect` 的右/下缘不含语义；
- `pet://` 路径防穿越（`..`、嵌套穿越、绝对路径、空路径全部拒绝）与 MIME 映射；
- 自定义协议 URL 的平台形式；
- JSONC 注释剥离（字符串里的 `//` 不被误剥、注释后的字段保留）；
- 从真实文件走完整加载路径（读文件 → 去 BOM → 剥注释 → 解析 → 校验）；
- **配置带 UTF-8 BOM 也要能读**（Windows 记事本与 `Set-Content -Encoding UTF8` 都会加 BOM；
  不加这一步就会报"第 1 行第 1 列解析失败"——实测把自己的配置文件改坏过一次）；
- **显示器几何一致性**：合法几何通过；长度不一致 / 工作区未被面板包含 / 空列表 /
  主屏零尺寸 四种坏几何全部被拒绝（与启动时的 `display::validate_geometry` 同一函数）。

**为什么不用 `cargo test`**：本机 GNU 工具链下 libtest 生成的测试可执行文件会被 Windows
加载器拒绝（`0xC0000139 STATUS_ENTRYPOINT_NOT_FOUND`）。已核对：测试程序与主程序的
**导入表逐项一致**（仅主程序多 `shlwapi.dll`），`WebView2Loader.dll` 的
`GetAvailableCoreWebView2BrowserVersionString` 导出也存在。
因此这是工具链/环境层面的问题，不是代码问题；改用独立 bin 来真正执行这些断言。

> 说明：`src-tauri/src/**` 里仍保留着 `#[cfg(test)] mod tests`（配置解析、指纹、物理校验、
> 矩形语义），它们在正常环境用 `cargo test` 就能跑；本机只是**跑不起来那个可执行文件**，
> 其断言内容已由本冒烟程序覆盖。

---

## 8. 未能验证的项（诚实记录）

| 项 | 为什么没验证 | 影响 |
| --- | --- | --- |
| 多显示器跨屏抛掷 | 本机只有一块屏 | 逐屏几何（工作区 + 面板按序成对）、`throwStepRegion` 的邻屏放行、启动期几何校验都已就位；需要双屏机器做一次观感回归 |
| 多宠物同屏与跨窗碰撞 | M1 范围 | — |
| 高 DPI（125%/150%）下的**观感** | 本机主屏为 100%（DPR 实测 = 1） | 缩放链路已有可判读的自检日志（`缩放: devicePixelRatio=…`），换屏后一眼就能看出是否需重定标；观感回归属 M1 |
| `tauri build` 出的 release exe | 本机只有一条 cargo 通道能联网，Tauri CLI 下载打包器仍不通，产不出安装包 | **M2 已补**：`cargo build --release` 的二进制已实跑（生产 CSP + `dist/` 资源 + 单实例 + 热重载，见 9.6）；**安装包（NSIS/MSI）仍未产出** |
| 真点击之后宠物是否仍可点 | 对穿实验第 2、3 轮连对照点都没反应。**原因已经查清**：本机 `SetCursorPos` 停不住光标（指针被桌面环境驱动），点下去时光标早已不在宠物身上 → 宠物处于穿透态、点击正确地穿了过去，是**测试方法**的问题，不是产品缺陷 | 无需修复；相关更正见 6.4 节。这条记录保留，提醒"别拿 `SetCursorPos` 的位置当基准" |

---

# 9. M2「像个正经应用」验证记录（2026-09-19）

M2 的六件事（托盘完整菜单 / 单实例 / 设置窗口 / 开机自启 / 配置热重载 / CSP 收紧）全部落地。
本节按"方法 → 证据 → 判读"记录，**两个只有真机运行才会暴露的 bug 单独写在 9.5**。

## 9.1 交付物与出口标准

| 交付物 | 位置 | 职责 |
| --- | --- | --- |
| 托盘 | `src-tauri/src/tray.rs`（从 `lib.rs` 拆出） | 显隐 / 回位 / 动作点播（按分类级联）/ 自启 / 设置 / 退出；配置热重载后重建 |
| 设置窗口 | `src-tauri/src/settings_window.rs` + `settings.html` + `src/settings-page.ts` | 独立的**普通窗口**（有边框、可聚焦、不置顶、整窗吃鼠标），页面只调自家命令 |
| 配置写回 | `config.rs::save_config` | 校验 → 备份 `config.jsonc.bak` → 写临时文件 → 原子改名 |
| 热重载 / 应用配置 | `src-tauri/src/reload.rs` | 指纹轮询 → 工作线程拆窗重建 → 重建托盘；设置保存走同一个落点 |
| 状态 | `state.rs` | 配置从普通字段改为 `Mutex<AppConfig>`（运行期可替换），读取统一走 `config_snapshot()` |
| CSP | `src-tauri/tauri.conf.json`（`csp` + `devCsp`） | 见 [`TAURI-CONFIG.md`](TAURI-CONFIG.md) 的逐项理由 |
| 排障探针 | `diagnostics.rs`：`WHALE_PET_DIAG_SETTINGS` / `WHALE_PET_DIAG_TRAY` | 自动化验证入口，默认不启用 |

**出口标准**（"不碰配置文件也能完成全部常用设置"）覆盖情况：

- 设置窗口可改：宠物**增 / 删**、名称、ID、尺寸、角落、边距、物理参数（重力/恢复系数/摩擦/顶部反弹/力度）、
  动画池（idle / turn / drag / clicks / moves 动作名 / 随机分类及其权重）、动画链权重、开机自启；
- 有意不做：`animations.events`（页面上只显示组数，原样保留）、`moves[].params`（保留原值，页面上标注"改请编辑配置文件"）；
- 未做：一键从 dsh-pet 导入（见 9.9）。

## 9.2 托盘完整菜单

**方法**：托盘的**真实点击注入不了**（本机桌面环境会持续把光标挪走，与 6.4/6.8 同一个限制），
但"点哪个菜单项"最终都落到同一个函数 `tray::handle_menu_event(app, id)` 的 match 分支上。
因此探针 `WHALE_PET_DIAG_TRAY=<id>[,<id>…]` **按 id 直接调那个函数**——
于是"菜单项 → 动作"这一层被真实执行，没被覆盖的只剩"muda 把系统点击派发进来"这一步。

**证据**（一次运行走 7 个菜单项，宿主侧 stderr 与宠物页诊断日志对照）：

```text
[托盘探针] 触发菜单项：pet-hide-all
[whale-pet] 托盘：隐藏全部宠物（1 只）
[托盘探针] 触发菜单项：pet-show-all
[whale-pet] 托盘：显示全部宠物（1 只）
[托盘探针] 触发菜单项：pet-home
[whale-pet] 托盘：显示全部宠物（1 只）
[pet-main-0] 菜单: 回到初始位置 (2116,100)（角落 top-right）      ← 宠物页真的执行了
[托盘探针] 触发菜单项：pet-anim:待机:待机呼吸休闲
[whale-pet] 托盘：点播动作 待机呼吸休闲
[pet-main-0] 菜单: 点播 待机呼吸休闲                              ← 点播链路通
[托盘探针] 触发菜单项：pet-settings
[whale-pet] 设置窗口已创建：1000×760
[托盘探针] 触发菜单项：pet-autostart  →  开机自启：已启用
[托盘探针] 触发菜单项：pet-autostart  →  开机自启：已关闭         ← 两次切换后注册表无残留
[托盘探针] 全部菜单项已走完
```

**判读**：

- 每个分支都执行到，且**效果落在它该落的地方**：显隐落在窗口上，"回到初始位置"与"点播"落在宠物页
  （托盘只当遥控器，动画链/朝向/气泡锚点仍在页面手里——与右键菜单同一条通道）；
- 回位落点 `(2116,100)` 与配置（`top-right`、marginX=24、marginY=100、size=420、主屏工作区 2560×1392）
  完全吻合：`2560-420-24 = 2116`，`100 = 100`；
- 动作点播的菜单 id 形如 `pet-anim:<分组>:<动画名>`，上面的日志证明"分组里带冒号解析"没出错；
- 托盘在配置热重载后会**重建**（分类可能变），启动与重建都没有失败日志。

## 9.3 单实例锁

**方法**：应用运行中再启动一次同一个 exe，看第二个进程的退出码与第一个进程的日志。

**证据**：

| 场景 | 结果 |
| --- | --- |
| debug 二进制，第二实例 | 4s 内 `HasExited=True ExitCode=0`，进程总数保持 1 |
| release 二进制，第二实例 | 立即 `HasExited=True ExitCode=0`（等待 0s），进程总数保持 1 |
| 第一实例日志 | `[whale-pet] 检测到第二次启动：显示已有实例（不再开新进程）` |

**判读**：重复启动不再产生"摸不到的幽灵宠物"，而是把已有实例显示出来（宠物窗 + 已打开的设置窗）。
这条也顺带解释了后面实验里的一个现象：**第二实例不会执行任何初始化**，
所以"想再起一个进程做实验"必须先杀掉已有实例。

## 9.4 设置窗口与"保存即生效"

**方法**：`WHALE_PET_DIAG_SETTINGS=1|save|autostart|addpet|delpet` 走的是**与托盘完全相同的**
`settings_window::open()`（因此建窗路径也被覆盖），随后由**页面自己**点按钮
（`eval` 到 `#btn-save` / `#autostart` / `#btn-add-pet` / `#btn-del-pet-1`），
而不是直接调 Rust 命令——这样"页面 → 命令 → 写盘 → 重建宠物窗"整条链路都被走到。

**证据一：窗口形态（进程外枚举）**

```text
标题='whale-pet 设置'  可见=True  位置=772,300  尺寸=1016x799  样式=0x14CF0000  扩展=0x40110
标题='Tauri App'       可见=True  位置=2116,100 尺寸=420x236   样式=0x14C80000  扩展=0x80C0138 ← 宠物窗
```

- 设置窗：`0x14CF0000` 含 `WS_CAPTION | WS_SYSMENU | WS_THICKFRAME | WS_MINIMIZEBOX | WS_MAXIMIZEBOX`；
  扩展样式 `0x40110` **不含** `WS_EX_TOPMOST(0x8)` / `WS_EX_TOOLWINDOW(0x80)` / `WS_EX_TRANSPARENT(0x20)`
  / `WS_EX_LAYERED(0x80000)` / `WS_EX_NOACTIVATE(0x08000000)` → 它是**普通窗口**，
  不参与桌宠那套穿透/置顶规则（宠物窗那一行对比可见 `L|T|NA` 三个位）；
- 尺寸 1016×799 = 内容区 1000×760 + 系统边框标题栏。

**证据二：五个动作各跑一次**

| 探针 | 关键日志 | 判读 |
| --- | --- | --- |
| `=1` | `设置页面加载事件 Finished url=…/settings.html` + `[settings] 设置: 已载入配置（1 只宠物，106 条素材）` | 建窗 + 页面加载 + `get_settings` 全通 |
| `=save` | `设置已保存：…config.jsonc（1 只宠物）` + `[热重载] 已应用（设置窗口保存）：1 只宠物，用时 513ms` + `[settings] 设置: 保存成功（1 只宠物，0 条素材警告）` | 写盘 + 立即生效 |
| `=autostart` | `[settings] 设置: 开机自启 → 启用` + 注册表出现 `whale-pet = …\whale-pet-desktop.exe` | 开机自启（测试后已删除该项） |
| `=addpet` | `已创建 2 个宠物窗口`：`pet-main-0` (2066,100) 与 `pet-pet2-1` (2026,1088)；`[热重载] 已应用（设置窗口保存）：2 只宠物，用时 1237ms` | 多开 |
| `=delpet` | 启动 2 只 → 页面删掉第 1 号 → `[热重载] 已应用（设置窗口保存）：1 只宠物，用时 555ms`，进程外只剩 1 个宠物窗 | 减员（旧的宠物窗与它的气泡/菜单窗一起被销毁） |

新宠物的落点校验：角落 `bottom-right`、`marginX = 40 + 1×24 = 64`、`marginY = 40` →
`x = 2560-470-64 = 2026`，`y = 1392-264-40 = 1088`，与实测 `(2026,1088)` 一致。

**证据三：写回没有丢东西（保存前 / 保存后字段对照）**

| 字段 | 保存前（磁盘） | 保存后 |
| --- | --- | --- |
| `pets[].size` | 470 | 470 |
| `marginX / marginY` | 24 / 100 | 24 / 100 |
| `position.corner` | top-right | top-right |
| `physics` gravity/restitution/friction | 1400 / 0.78 / 2.5 | 1400 / 0.78 / 2.5 |
| 动画池 | **磁盘上这个段根本不存在**（M0 时代的精简配置，加载时由 `migrate_config` 用内置模板在**内存里补齐**） | 补齐后的完整池被显式写回（3 个移动动作、5 个分类、events、权重） |

**写回策略（有意取舍，务必备忘）**：

1. **JSON 不存注释**，而内置模板是带大量注释的。设置窗口保存会**整体重写**为规范化 JSON，
   文件开头写一段说明（这份是谁写的、备份在哪、字段含义去哪查、手写改动会被下一次保存替换）；
2. 保存前把上一版备份成 `config.jsonc.bak`（只留最近一次）；先写 `config.jsonc.tmp` 再原子改名，
   避免热重载读到"写了一半"的文件（实测：`config.jsonc` 3499 → 6116 字节，`config.jsonc.bak` = 上一版）；
3. 想保住手写注释的用户，就不要从设置窗口保存（直接改文件即可，热重载会接住）。

## 9.5 两个只有真机运行才会暴露的 bug

> 这两个都不是单元测试能发现的：一个需要"配置里恰好有 `HashMap`"，
> 另一个需要"运行期真的把窗口全拆了"。它们都属于**进程生命周期**层面的问题。

### 9.5.1 启动约 1 秒后自己重建一次（每次启动都发生）

- 现象：启动日志里紧跟"配置热重载已就绪"之后出现 `检测到配置文件被修改，开始应用`。
- 根因：判断"配置有没有变"用的是 `serde_json::to_string()` 字符串比较，
  而 `animations.events` 是 `HashMap`——**它的序列化顺序每进程每时刻都不同**，
  于是"读到同一份配置"永远被判成"变了"。
- 修法：`config::canonical_json()`（递归按键名排序）比较语义；并在启动时先建立一次文件指纹基线
  （`reload::mark_stamp_current`），连"第一拍拿 `None` 去比"的机会都不留。
- 附带发现：**设置窗口保存后也会白重建一次**（同一条比较逻辑），一并修掉。

### 9.5.2 改一次配置，应用直接退出（用户会看到"宠物消失、托盘也没了"）

- 现象（修复前的真实日志）：

  ```text
  [whale-pet][热重载] 检测到配置文件被修改，开始应用
  [whale-pet] 应用退出                     ← 拆完旧窗、新窗还没建起来的那一瞬间
  [whale-pet] 窗口 pet-main-0：原点=…
  ```

- 根因：重建是"先拆旧窗 → 再建新窗"，**拆完的那一瞬间窗口数为 0**，
  Tauri 据此认为"最后一个窗口被关闭"，发出 `RunEvent::ExitRequested`；没人拦，进程直接退出。
  没有设置窗在场时 100% 复现；设置窗开着时窗口数不归零，所以之前几次实验"看起来正常"。
- 修法：在 `run` 的 `ExitRequested` 分支里**只拦一种情况**——`reload::is_applying()` 为真（正在重建）时
  `api.prevent_exit()`。注意 `AppHandle::exit()`（托盘"退出"走的正是它）**也会触发 `ExitRequested`**，
  所以托盘退出前先立 `QUITTING` 旗，否则会变成"点退出退不掉"。
- 验证（修复后，**故意在没有设置窗的情况下**）：

  ```text
  [whale-pet][热重载] 检测到配置文件被修改，开始应用
  [whale-pet] 已拦截一次误退出：重建窗口期间窗口数瞬时归零（配置热重载/保存设置的正常现象）
  [whale-pet] 窗口 pet-main-0：原点=(2066,100) 尺寸=470×264 …      ← 新窗建起来了
  [whale-pet][热重载] 已应用（配置文件被外部修改）：1 只宠物，用时 538ms
  ```

  进程存活、托盘仍在、`退出` 仍可用（9.2 的探针里 `pet-autostart` 等分支正常，且 9.3 的
  单实例日志说明进程没有被误拦在退出流程里）。

### 9.5.3 配置热重载的实测数据

| 改动 | 生效用时 | 新窗口落点（size 470） |
| --- | --- | --- |
| 420 → 470（有设置窗在场） | 517ms | (2066,100) 470×264 |
| 470 → 430（有设置窗在场） | 483ms | (2106,100) 430×242 |
| 430 → 470（**无**其他窗口，触发退出拦截） | 538ms | (2066,100) 470×264 |
| release：420 → 460 | — | (2076,100) 460×259 |

落点校验：`2560 - size - 24`（右缘对齐、marginX=24），高度 `size×9/16`。

## 9.6 CSP 收紧与 release 二进制首次实跑

**方法**：`cargo build --release`（5 分 01 秒）。release 是 GUI 子系统
（`#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]`），
**stdout/stderr 不可见**——所以 release 的证据只用两样：前端诊断日志（`pet-debug.log`，
它经 IPC 写文件，本身就是"IPC 通不通"的证据）与进程外窗口枚举。

**证据**（production `csp` + `dist/` 资源）：

```text
[pet-main-0] 启动: 页面加载完成，assetBaseUrl=http://pet.localhost
[pet-main-0] 动画: 首帧就绪 readyState=4 src=http://pet.localhost/webm/待机呼吸休闲.webm
[pet-main-0] 窗口: 初始 原点=(2116,100) 尺寸=420x236 → 包围盒=(2116,100) 宿主可交互=true
[settings]   设置: 已载入配置（1 只宠物，106 条素材）
[settings]   设置: 保存成功（1 只宠物，0 条素材警告）
（外部改 size=460 后）
[pet-main-0] 窗口: 初始 原点=(2076,100) 尺寸=460x259 → 包围盒=(2076,100)
```

**判读**：

- `assetBaseUrl=http://pet.localhost` + `readyState=4` → 自定义协议在**生产 CSP** 下仍被放行
  （`media-src` 那一项是对的；这类问题不修的表现是"宠物不见了但没有任何报错"，属于最难查的一类）；
- 所有页面日志都经 `pet_debug_log` 命令落到文件 → `connect-src ipc: http://ipc.localhost` 是对的
  （少了它的表现是"所有命令静默失败"）；
- 设置窗口在生产 CSP 下也能打开、取配置、保存并热重载；
- release 下四个场景（无探针、设置、保存、热重载）进程都存活，没有再触发 9.5.2 那条退出路径。

## 9.7 纯逻辑冒烟（51 项，`cargo run --bin logic-smoke`）

M2 新增 `[10]` 段共 12 项断言，覆盖设置窗口写盘路径里**不需要窗口**的那部分：

```text
[10] 保存配置（设置窗口写盘路径）
  [通过] save_config 返回写入路径 / 写入字节数非零 / 保存后的文件带说明注释头
  [通过] 首次保存没有备份（原来没有文件才不备份） / 第二次保存仍返回备份路径
  [通过] 保存后的文件可被 load_config 读回
  [通过] 读回的配置与保存前语义一致            ← 用规范 JSON 比较，避免 HashMap 顺序误报
  [通过] 连续两次 round-trip 完全稳定（迁移幂等）
  [通过] 备份里是上一版（改动前的 size） / 当前文件是新版（改动后的 size）
  [通过] 非法配置被 save_config 拒绝 / 被拒绝时配置文件未被改动
=== 结果：通过 51 项，失败 0 项 ===   （退出码 0）
```

其中"**非法配置被拒绝时配置文件一个字节都没变**"这条是设置窗口的安全底线：
用户点一次"保存"绝不能把配置改成"下次起不来"的样子。

## 9.8 开发期副作用修复（不是产品功能，但会咬人）

Vite 的 watcher 在 Windows 上会遇到"编辑器/脚本做原子写留下的
`.<文件>.<pid>.<uuid>.tmpdir/` 临时目录"并抛 `EBUSY`，**直接把 dev server 打挂**。
实测被打挂两次（第一次在 `src-tauri/src` 下、第二次在 `docs/` 下），
表现为"改了一个文件，前端热更新服务整个退出"。
修法：`vite.config.ts` 的 `server.watch.ignored` 排除 `src-tauri`、`.*.tmpdir`、`.git`、`node_modules`、`dist`。
验证：在 `src-tauri/src` 下制造同款临时目录并删除，dev server 存活（1420 端口仍在监听）。

## 9.9 未能验证 / 残留风险（诚实记录）

| 项 | 情况 |
| --- | --- |
| 托盘图标的**真实点击** | 注不进去（本机鼠标注入不可靠，见 6.4）。覆盖到"菜单项 → 动作"这一层为止；**未覆盖**：muda 把系统点击派发进来这一步，以及托盘菜单的视觉/级联层级（系统原生菜单，不经 webview，本机也没有可靠截图手段） |
| 高 DPI（125% / 150%） | 仍是单屏 100%（DPR=1），与 M1 相同，未变 |
| 多显示器 | 仍是单屏，未变 |
| 安装包（NSIS / MSI） | **已产出**（v0.1.0 首次出包，见第 14 节）。release 二进制亦已实跑（9.6） |
| 从 dsh-pet 一键导入 | 未实现（`ROADMAP.md` 已注明；动画素材导入已有 `scripts/import-animations.ps1`） |
| **一次未复现的崩溃事件** | 2026-09-19 12:50:41 有一条 `Application Error`：release 二进制、异常码 `0xc0000409`（release 下 panic 走 abort 的 fail-fast 码）、进程存活 < 1 秒。之后 4 次 release 运行（无探针 / 设置窗 / 保存 / 热重载）都**没有新增事件**，未能复现，也未能与任何一次启动动作对应上。**保留为已知未解释项**：若它在真实使用中再现，应优先按"启动即崩"的路径排查（`tauri.conf.json` 嵌入资源、插件初始化顺序） |

---

# 10. 外观与结构升级验证记录（2026-09-19）

起因是用户对 M2 成品的三条反馈：**托盘菜单样式不好看**、**应用没有图标**、
**设置界面杂乱且没有导航栏**，并要求"单只宠物的行为应当属于它自己"。

四条改动全部落地：应用图标（代码绘制）、自绘托盘菜单、设置界面重做（左侧导航 + 跟随系统深浅色）、
每只宠物可独立覆盖动画池与权重。

## 10.1 交付物

| 交付物 | 位置 | 说明 |
| --- | --- | --- |
| 图标绘制 | `src-tauri/src/icon_art.rs` | SDF + 解析抗锯齿，一份代码产出 `.ico` 与运行时托盘图标 |
| 图标生成工具 | `src-tauri/examples/make-icon.rs` | `cargo run --example make-icon`：写 `icons/` 全套 + 候选对比图（png/ico 走 dev-dependencies，**不进应用二进制**） |
| 图标产物 | `src-tauri/icons/` | `icon.ico`（16/24/32/48/64/128/256）、`icon.png`、`32x32/128x128/128x128@2x`、`tray-16/32.png`、`candidates.png` |
| 托盘菜单窗 | `src-tauri/src/tray_menu.rs` + `tray-menu.html` + `src/tray-menu-page.ts` | 自绘菜单：锚在托盘图标附近、外点关闭、按内容变高、级联交给滚动列表 |
| 托盘 | `src-tauri/src/tray.rs`（重写） | 左键 = 显隐切换；右键 = 弹自绘菜单；**原生菜单与开机自启项移除** |
| 设置界面 | `settings.html` + `src/settings-page.ts`（重写） | 左侧导航 + 每宠独立页 + 通用页 + 跟随系统深浅色 |
| 每宠行为 | `src-tauri/src/config.rs` | `pets[].animations` / `pets[].animationWeights` 可选覆盖 + `effective_*` 解析 |
| 截图探针 | `scripts/capture-window.ps1` | 屏幕合成截图（WebView2 内容 `PrintWindow` 抓不到） |

## 10.2 应用图标

**方法**：图标不是位图素材，而是"一份绘制代码"：`icon_art::render(size, style, detail)`。
两个消费方——`examples/make-icon.rs` 产出 `.ico`/`.png`，托盘在运行时直接渲染 RGBA 交给
`tauri::image::Image`——因此**只有一个事实来源**（改一个常量两处同时变），
也不必往仓库里塞二进制块或引入 png 解码器。

绘制用的是 SDF + 解析抗锯齿（覆盖率 = `clamp(0.5 - d·size, 0, 1)`），
而不是 4× 超采样：1024² 的 4 倍缓冲要吃 268MB，没必要。

**判读（三版迭代，都是"渲染出来看一眼"改的）**：

| 版本 | 观察 | 改动 |
| --- | --- | --- |
| v1 | 尾鳍又小又竖，读起来是**鱼**；嘴像一撇胡子 | 尾鳍加大、嘴改弧 |
| v2 | 尾鳍仍竖向分叉（鱼尾）；背鳍被并进身体看不见 | 尾叶改**横向宽扁**、留中缝；背鳍外移凸出躯干；整体加 7° 上浮倾角 |
| v3 | 尾柄太细，尾叶像"掉"在身体外面 | 尾柄加粗到 0.052；重心下移；托盘版单独加粗尾叶（16px 下细尾叶会被抗锯齿抹平） |

**证据一：exe 里真的嵌进去了**（进程外从二进制提取，不是"我以为写了"）：

```text
PrivateExtractIconsW(exe, 0, 256, 256) → 1 个图标资源
提取出来的 256×256：白色小鲸鱼 + 粉色渐变圆角底（与 icons/icon.png 一致）
ExtractAssociatedIcon(exe) → 32×32 同样是小鲸鱼
```

**证据二：托盘那份来自同一份绘制代码**：`tray.rs` 用 `icon_art::tray_rgba(32)`（`Detail::Tray`：
省掉气泡与嘴、尾叶加粗、眼睛放大）。`icons/tray-16.png` 与 `tray-32.png` 是同一函数的输出快照。

## 10.3 自绘托盘菜单

**为什么要自绘**：Windows 原生菜单由系统绘制，圆角、间距、图标、强调色、深浅色**全都改不了**；
而设置窗口是自绘的，两处风格不一致会显得拼凑。代价是要自己扛三件事（都实现了）：

1. **摆位**：锚在光标（= 点托盘图标时的位置），优先出现在光标上方，上方不够就翻到下方，
   最后整体夹进"点击那一块屏"的工作区；
2. **外点关闭**：窗口 `focusable(false)`（与宠物窗同一纪律，绝不抢焦点），拿不到失焦事件，
   改由宿主光标轮询判定"在菜单矩形外按下"；
3. **按内容变高**：主菜单 272×286，"动作点播"展开后页面量完高度调 `resize_tray_menu`，
   宿主用**记住的锚点**重算位置（否则窗口会跳走）。

**内容（按用户要求精简）**：

```text
🐳 whale-pet / 小鲸鱼 · 1 只
────────────────────────────
👁  隐藏宠物 / 显示宠物      ← 一个切换项（文案随状态变）
🏠  回到初始位置
✨  动作点播  ›
────────────────────────────
⚙  设置…
⏻  退出
```

开机自启**从托盘移除**，只留在设置窗口的「启动与系统」页（"设一次就不动"的开关不该占常驻菜单）。

**证据一：界面（截图探针，浅色）**

```text
scripts/capture-window.ps1 -TitleLike '托盘菜单'
→ 窗口「whale-pet 托盘菜单」308x298 @ 359,914
（展开动作点播后：308x455 / 双宠时 308x493，顶部出现宠物 chips）
```

白色圆角卡片 + 鲸鱼 logo + 图标 + 分隔线；"动作点播"展开后是分类做小标题的可滚动列表。

**证据二：动作层（走真实代码路径）**。菜单项点击最终落到**唯一**入口
`tray::run_menu_action`（自绘菜单页经 `tray_menu_action` 命令调它，探针也调它）：

```text
[托盘探针] 触发菜单动作：toggle        → [whale-pet] 托盘：隐藏全部宠物（1 只）
[托盘探针] 触发菜单动作：home          → [pet-main-0] 菜单: 回到初始位置 (2116,100)（角落 top-right）
[托盘探针] 触发菜单动作：anim:待机呼吸休闲 → [pet-main-0] 菜单: 点播 待机呼吸休闲
[托盘探针] 触发菜单动作：toggle        → [whale-pet] 托盘：显示全部宠物（1 只）
[托盘探针] 触发菜单动作：settings      → [whale-pet] 设置窗口已创建：1000×760
```

**证据三：双宠时按宠物点播**：设置里加一只宠物并保存（`已创建 2 个宠物窗口`）后，
菜单状态变成 2 只，动作点播顶部出现宠物 chips（`小鲸鱼` 选中态 / `宠物 2`），
切换 chips 即切换目标宠物的动画池。

## 10.4 设置界面重做

**用户原话**："设置的 ui 界面不好看，而且感觉很杂乱，也没有导航栏"。
旧界面是"一长条表单往下滚"，截图里能直接看出三个问题，这一版逐个改掉：

| 问题 | 处理 |
| --- | --- |
| 没有信息层级 | 左侧竖向导航：宠物逐个列出（有自定义行为的打小圆点）+ 通用分组（物理参数 / 动画池默认值 / 启动与系统 / 关于） |
| **勾选项被挤成竖排文字**（截图里最扎眼的一处） | 勾选行改成独立的横向 flex 行 `.check-row`，不再塞进窄网格单元 |
| 同一行输入框高低不齐 | 网格改 `align-items: start`，且说明位恒定存在（没有就留空），字段结构一致 |
| 深色一股"命令行"味 | 跟随系统的浅色/深色双主题（`prefers-color-scheme` + 一套变量），参考图的粉玫瑰强调色（`#ec6f9b`）只用在选中/焦点/主按钮上 |

**证据（逐页截图，浅色）**：宠物页 / 物理参数 / 动画池默认值 / 启动与系统 四页各自渲染正常；
导航选中态有粉色底 + 左侧指示条；底部常驻状态栏显示"已载入：1 只宠物 · 素材 106 条"
与「保存并立即生效」按钮。截图由 `scripts/capture-window.ps1` 抓取（见 10.6）。

## 10.5 每只宠物独立的动画池

**语义（刻意选的）**：`pets[].animations` / `pets[].animationWeights` 是**整体覆盖**，
不是逐字段合并。合并看起来更聪明，但"我删掉了某个动作它还在播"这类问题在合并语义下无法解释
（删除与"未设置"不可区分）。不写覆盖就跟随全局——全局那份的角色变成"新宠物的默认值"。

**实现**：`AppConfig::effective_animations/effective_weights`（`pet_window::create_one` 用它下发
`PetConfigDto`，于是宠物页、右键菜单、托盘动作点播拿到的都是**这只宠物实际生效**的池）；
校验与素材警告都按"全局 / 某只宠物"两个作用域分别报错。

**证据一：纯逻辑冒烟（60 项全绿，新增 9 项）**

```text
[11] 每宠独立动画池
  [通过] 没写覆盖的宠物用全局池
  [通过] 写了覆盖的宠物用自己的池
  [通过] 覆盖是整体替换：全局权重不参与
  [通过] 自定义行为可被识别（UI/托盘用它打角标）
  [通过] 全局改动不影响已覆盖的宠物
  [通过] 带覆盖的配置整体校验通过
  [通过] 素材警告带宠物作用域
  [通过] 覆盖写坏了会被校验拒绝 / 拒绝信息里指出是哪只宠物
=== 结果：通过 60 项，失败 0 项 ===
```

**证据二：端到端（真机）**

1. 设置窗口点「行为 → 单独设置」（自动从全局复制一份作起点）→ 保存：
   `[热重载] 已应用（设置窗口保存）：1 只宠物，用时 484ms`，
   配置里出现 `pets[0].animations`（完整池）✓；
2. 手工把 `pets[0].animations.idle` 改成 `["蓝鲸现世"]`（全局仍是 `["待机呼吸休闲"]`），
   热重载后宠物页日志：

   ```text
   [pet-main-0] 配置: size=420 idle="蓝鲸现世" click="点击回应-开心跃动" hitBox=131,33 158x187
   [pet-main-0] 动画: 首帧就绪 readyState=4 src=http://pet.localhost/webm/蓝鲸现世.webm
   ```

   即：**它真的用了自己那份池**，而全局池没有被动过。

## 10.6 新增的验证工具

| 工具 | 用途 |
| --- | --- |
| `scripts/capture-window.ps1` | 按窗口标题抓**屏幕合成截图**（WebView2 内容 `PrintWindow` 抓不到、返回全黑；`CopyFromScreen` 抓的是 DWM 实拍）。先提窗口到前台再抓，因此**别拿它当"窗口是否被遮挡"的证据** |
| `WHALE_PET_DIAG_TRAY_MENU=<ms>[:picker]` | 按延时弹出托盘菜单（走真实 `tray_menu::show()`），供截图；`:picker` 再展开动作点播 |
| `WHALE_PET_DIAG_TRAY=<动作>[,…]` | 按序走托盘菜单动作层（`toggle` / `home` / `anim:<名>` / `settings`） |
| `WHALE_PET_DIAG_SETTINGS=…` | 扩充了 `ownbehaviour` 与 `nav:<页>`（逐页截图用） |

> 注意：`capture-window.ps1` 必须存成 **UTF-8 with BOM**。Windows PowerShell 5.1 按 ANSI 读取
> 无 BOM 的 .ps1，中文注释会把引号"吞掉"，报 `TerminatorExpectedAtEndOfString`（实测踩过）。

## 10.7 未能验证 / 残留风险（本轮）

| 项 | 情况 |
| --- | --- |
| 托盘图标的**真实点击** | 仍然注不进去（桌面环境持续挪走光标）。覆盖到"动作层"为止；`show()` 的摆位逻辑靠探针 + 截图验证（弹出位置/尺寸/锚点日志齐全） |
| 托盘图标在通知区**是否可见** | 未验证：Windows 11 默认把新图标收进"隐藏的图标"浮出层，截图抓不到。若用户看不到，需要在任务栏设置里把它设为显示 |
| 深色主题的实际观感 | 主题由 `prefers-color-scheme` 切换，本机桌面为浅色；深色那套变量**未被真机渲染验证过**（逻辑上只是换一套变量） |
| 视觉验收 | 界面好不好看最终由用户判断；本轮已把四个设置页 + 两种托盘菜单状态截图存档，见 [`screenshots/`](screenshots/README.md) |
| 桌宠窗压在设置窗上方 | 宠物是 `always_on_top`，所以它会浮在设置窗之上（截图里能看到它挡着右上角按钮）。这是预期行为，不是 bug；若觉得碍事，可以给设置窗也加 topmost 或在打开设置时临时把宠物降下去 —— 待用户决定 |

---

# 11. 图标重做与托盘菜单折叠（2026-09-19）

用户看过第 10 节那一版后的两条反馈：

1. "各种图标非常丑，以'桌宠'为核心选择/生成图标类型"；
2. "托盘菜单里动作点播的分类默认收缩，点击后再展开具体内容"。

## 11.1 图标：从"代码画 SDF"换成"矢量 SVG 作源"

第 10 节的图标是**用 Rust 拼 SDF 图元**（椭圆 + 圆角矩形）画的。它的好处是"一份代码两个用途"，
但**观感上限很低**：圆润造型、渐变、柔和投影、统一笔画这些"让它不难看"的东西拼不出来——
用户一句"图标丑"就是这个原因。

现在：

| 层 | 做法 |
| --- | --- |
| 图形源 | **手写 SVG**（`src-tauri/icons/design/app-icon.svg`，候选在 `design/candidates/`） |
| 栅格化 | `cargo run --example make-icon` → 用 **resvg** 生成 PNG / 多尺寸 ICO / 托盘 RGBA |
| 应用侧 | 托盘图标 `include_bytes!("../icons/tray-32.rgba")`（4KB 预生成裸数据） |
| 依赖 | resvg/usvg/png/ico 全在 `[dev-dependencies]`，**应用二进制不背 SVG 渲染器** |

**图标候选（六款；用户追问"图标为什么一定鲸鱼"，于是补了三款不指向物种的）**：

| 候选 | 概念 | 是否指向具体物种 | 小尺寸表现 |
| --- | --- | --- | --- |
| `a-peek` | 鲸鱼趴在**任务栏**上探头 | 鲸鱼 | 16px 糊成"横杠 + 头" |
| `b-face` | 鲸鱼**大脸**（腮红 + 微笑 + 水柱） | 鲸鱼 | 最容易认出"是个活物" |
| `c-paw` | 窗口里一枚**爪印** | 不指向 | 清晰，但偏通用 |
| **`d-pet-peek`（当前采用）** | **长耳朵的小生物**趴在任务栏上探头 | **不指向**（耳朵只说"是只宠物"） | 耳朵 + 眼睛 + 横条，16/32px 都能读出来 |
| `e-paw-tile` | 大爪印独占整块底 | 不指向 | **最清晰**（单一形状） |
| `f-cursor-pet` | 小生物 + **鼠标指针**（"用鼠标跟它互动"） | 不指向 | 元素略多，16px 稍挤 |

> **"为什么一定鲸鱼"这个问题问得对**：图标要表达的是"桌面上养了只宠物"这件事本身，
> 而素材是可替换的（配置里引用任意 webm），不该把图标锁死在某一种动物上。
> 现在默认用的是**不指向物种**的 `d-pet-peek`；想换任意一款：
> `cargo run --example make-icon -- --svg design/candidates/<名字>.svg`。
> 六款的 256/64/32/16 对比图见 [`screenshots/icon-candidates.png`](screenshots/README.md)，
> 图里"当前采用"的那一列下方有粉色标记线（判据是**文件内容**而不是路径——
> 正式图标是从候选复制来的，按路径比永远标记不上）。

**用户选定（2026-09-19）**：第四款 `d-pet-peek`（长耳朵小生物趴在任务栏上）——
用户看完候选图后回"是这个"。

> **一个自我纠正**：我在用户点头**之前**为了 16px 可辨识度，私自把耳朵立高、任务栏压薄了一版，
> 而用户是对着**改动前**那张图选的。以用户的选择为准：**已把正式图标还原成他选的那一版**
> （耳朵圆一点、任务栏厚一点），立耳版另存为 `design/candidates/g-pet-peek-tall-ears.svg` 作备选。
> 教训：**用户点头的那张图就是验收基准**，之后任何"顺手优化"都算改了需求，应当回到用户确认的版本
> 或在征求同意后再改。

`design/app-icon.svg` 与 `d-pet-peek.svg` **逐字节相同**，因此 exe 的 `.ico`、任务栏/标题栏图标、
运行时托盘图标（`tray-32.rgba`）、打包配置用的都是这一款；已重新生成并**从 exe 二进制里提取核对**过。

**证据：新图标真的进了 exe**（从二进制提取，不是"我以为嵌了"）：

```text
PrivateExtractIconsW(exe, 0, 256, 256) → 1 个资源
提取结果：青绿渐变圆角底 + 白色小生物（耳朵/腮红/微笑）趴在白色任务栏上
```

> **踩坑记录**：改完 `icons/icon.ico` 直接 `cargo build` **不会**更新 exe 里的图标——
> 图标是**构建脚本**（tauri-build）嵌进去的，而构建脚本只在 `tauri.conf.json` 变化时重跑。
> 实测第一次提取出来还是旧图标。正确做法：改图标后把 `tauri.conf.json` 的修改时间碰一下
> （或 `cargo clean -p whale-pet-desktop`）再构建。

## 11.2 菜单/界面图标：改用 Lucide（ISC）

第 10 节的线性图标是手画的 16×16 路径：粗细不匀、齿轮画得像"太阳"、星星形状随意——
这也是"图标丑"的一部分。现在换成 **[Lucide](https://lucide.dev)（ISC 许可）** 的 24×24 线性形状，
统一 2px 圆头笔画，**只把用得到的形状内联进代码**（不引依赖、不联网、CSP 友好，颜色跟随 `currentColor`）：

| 位置 | 图标 |
| --- | --- |
| 托盘菜单 | `eye` / `eye-off`（显隐切换）、`house`（回到初始位置）、`sparkles`（动作点播）、`settings`、`power`、`chevron-right` |
| 设置导航 | `paw-print`（宠物）、`sliders-horizontal`（物理参数）、`clapperboard`（动画池）、`rocket`（启动与系统）、`info`（关于）、`plus`（添加） |
| 设置按钮 | `folder-open`（打开配置文件）、`rotate-ccw`（放弃改动）、`save`（保存并立即生效）、`x`（删除） |

两处页头的小鲸鱼 logo 也按新图标重新手写了一遍（与 `app-icon.svg` 同造型）。

## 11.3 动作点播：分类默认收起，点击展开

用户要求"分类默认收缩，点击后再展开具体内容"。现在的行为：

- 打开动作点播只列**分类**（`待机 1 / 点击回应 5 / 小动作 18 / 玩耍 27 / 吃什么 12 / 时节 21 / 文字 2`），
  每个分类右侧显示条数与"无素材"提示；
- 点分类标题才展开它的动作（chevron 旋转 90°），**展开/收起都会让窗口自动变高变矮**
  （宿主按记住的锚点重算位置）；
- 标题栏另有一个「全部展开 / 全部收起」开关；**每次重新弹出菜单都会回到全部收起**。

**证据（截图 + 探针）**：

```text
模式=picker          → 308x338：只列 7 个分类（截图 screenshots/tray-menu-picker.png）
模式=picker-expanded → 308x374：待机展开列出动作，其余仍收起（screenshots/tray-menu-picker-expanded.png）
```

## 11.4 侧栏对齐修复（用户："宠物模块下面和通用下面的水平位置不一致"）

第 10 节的侧栏给宠物项加了 `.nav-item.sub`（左缩进 22px），而通用项是 10px；
加了图标之后这个 12px 的差别非常显眼——**宠物那一组的图标整体比通用组靠右**。
现在删掉 `sub` 这一层缩进：两组用同一套左内边距，图标与文字落在同一条竖线上。
顺手把分组标题（"宠物"/"通用"）的左内边距从 8px 调成 10px，与下面条目的左边对齐
（差 2px 也会显得"没对齐"）。截图见 [`screenshots/settings-pet.png`](screenshots/README.md)。

## 11.6 漏改的两处 logo（用户："这两处的图标都没改"）

用户截图圈出**设置侧栏顶部的品牌 logo** 与**托盘菜单头部的 logo**——它们还是旧的粉色小鲸鱼。
原因很直白：应用图标换了，但这两处 logo 不在图标文件里，而是**手写在页面里的内联 SVG**
（`settings-page.ts` / `tray-menu-page.ts` 的 `PET_LOGO` 常量 + 两处 CSS 渐变底色），换图标时漏了。

现在三处 logo 全部改成与 `design/app-icon.svg` 同一造型（青绿底 + 趴在横条上的小生物 + 腮红/微笑）：

| 位置 | 实现 | 证据 |
| --- | --- | --- |
| 设置窗口侧栏品牌 logo | `settings.html` 的 `.brand .logo` 底色 + `settings-page.ts::PET_LOGO` | [`screenshots/settings-pet.png`](screenshots/README.md) |
| 设置窗口「关于」页的大 logo | 同一个 `PET_LOGO` + 内联底色 | 同上（同一份常量） |
| 托盘菜单头部 logo | `tray-menu.html` 的 `.logo` 底色 + `tray-menu-page.ts::PET_LOGO` | [`screenshots/tray-menu.png`](screenshots/README.md) |

> **下次换图标要一起改的地方**（这一条是被用户抓出来的，写下来避免再漏）：
> ① `icons/design/app-icon.svg`（图标源）→ ② `cargo run --example make-icon` 生成产物
> → ③ 把 `tauri.conf.json` 碰一下（否则 exe 图标不更新）→ ④ 两个页面里的内联 `PET_LOGO`
> 与它们的 CSS 渐变底色。已用 `Select-String` 全仓搜过旧图标特征（`24.4` / `f8749f` / `ffc2db` / `25304C`），
> 确认没有残留。

---

# 12. 深色主题真机验证 + 崩溃压力复现（2026-09-19）

第 10/11 节留下的两笔"写了但没验证"，本轮清掉。

## 12.1 深色主题：从"写了没人看"到有截图

**问题**：深色那一套 + `@media (prefers-color-scheme: dark)` 早就写好了，但开发机桌面是浅色，
所以它一直处于"存在但从未被渲染过"的状态——变量漏一个、对比度不够，谁也不知道。

**做法**（两层）：

1. **变量表改用 CSS `light-dark()`**：深/浅两套值写在**同一行**的两个位置
   （`--bg: light-dark(#ffffff, #1c1e23)`），`color-scheme` 决定取哪一边。
   这样"深色那份漏改一个变量"这类错误从结构上就没了（原来是两份变量表，靠人工同步）；
2. **加显式主题覆盖**（`?theme=dark|light`）：页面启动时读 URL 参数写 `data-theme`，
   CSS 里 `:root[data-theme='dark'] { color-scheme: dark }` 即可强制。
   只为自检存在，正常运行路径完全不受影响（默认仍跟随系统）。
   Rust 侧由 `WHALE_PET_DIAG_THEME=dark` 在创建窗口时把参数带进 URL。

**证据**：日志里现在会打印**生效主题**，两张截图存档。

```text
[settings]  设置: 已载入配置（1 只宠物，106 条素材；主题=dark）
[tray-menu] 托盘菜单: 状态已刷新（1 只宠物，可见=true；主题=dark）
```

| 截图 | 内容 |
| --- | --- |
| [`screenshots/settings-dark.png`](screenshots/README.md) | 设置窗口深色：深底 + 卡片 + 粉色强调 + 侧栏选中态，对比度正常 |
| [`screenshots/tray-menu-dark.png`](screenshots/README.md) | 托盘菜单深色：面板、文字、图标、分隔线都正常 |

> 试过的弯路：先用了 WebView2 的 `--force-dark-mode`（想借它翻转 `prefers-color-scheme`），
> 实测页面判定仍是 `light`——那条路不可靠，才改成"显式 `data-theme` + `light-dark()`"。
> 另外**深色下"跟随系统"仍是默认行为**：`?theme=` 只是给自检开的后门，不是产品功能。

## 12.2 那次未复现的崩溃：压力跑了两组，仍未复现

第 9.9 / 10.7 节记过一次 `0xc0000409`（release 二进制、存活 < 1 秒）的孤立事件。
本轮用**当前代码**重新构建 release 后跑了压力：

| 组 | 内容 | 结果 |
| --- | --- | --- |
| A | 冷启动 ×10（每次 2.5s 后强杀） | 异常退出 **0** 次；`Application Error` 事件数不变（仍是 12:50:41 那一条） |
| B | 启动 → 连续改配置触发热重载 ×5（release 下 `panic=abort`，最容易暴露） | 进程一直存活；新增崩溃事件 **0** |

**结论**：仍未复现，事件保持"孤立、无法归因"。记录保留在"未能验证"清单里——
如果将来在真实使用中再现，优先按"启动即崩"排查（`tauri.conf.json` 嵌入资源、插件初始化顺序）。

## 12.3 一个环境坑（只影响自动化验证）

反复出现 "`Port 1420 is already in use`"：`job_kill` 杀掉的只是 pwsh 包装进程，
**Vite 的 node 子进程还活着占着端口**，于是新起的 dev server 起不来、页面加载成"拒绝连接"。
现在验证流程统一改成"**先按端口精确清理**再起服务"：

```powershell
foreach ($pid in (Get-NetTCPConnection -LocalPort 1420 -State Listen).OwningProcess) { Stop-Process -Id $pid -Force }
```

（这是本机自动化的事，不影响 `pnpm tauri:dev` 的正常使用。）
## 11.7 顺带修掉的两个验证工具问题
| 问题 | 处理 |
| --- | --- |
| 截图脚本按标题匹配到**别的应用**（"设置"二字撞车），把无关窗口提到前台截了一张别人的图 | `capture-window.ps1` 改为收集**所有**匹配窗口后取**面积最大**的那个 |
| 弹出自绘菜单后等一两秒截图，期间桌面环境的零星鼠标按下会触发"外点关闭"，截图变成"窗口不存在"（撞两次） | 探针模式下调用 `tray_menu::keep_open_for_probe()` 关掉自动关闭，保证"弹出来了就能拍到"；正常路径不受影响 |


---

# 13. M3 第一阶段：LLM 能力（碎碎念 / 对话）验证记录（2026-09-19）

设计说明见 [`LLM.md`](LLM.md)。这一阶段的目标是"**默认零联网、开启后可用、密钥安全、
有不依赖真实 key 的验证手段**"，四件事都做到了，而且证据全部可在本机复现。

## 13.1 交付物

| 交付物 | 位置 | 说明 |
| --- | --- | --- |
| 适配层 | `src-tauri/src/llm.rs` | OpenAI 兼容请求构造 / 响应解析 / 结构化失败；ureq + native-tls；**在 Rust 侧发请求** |
| 配置 | `src-tauri/src/config.rs` 的 `llm` 段 | 默认全关；provider / model / baseUrl / 温度 / 超时 / 两个功能开关 / 周期 / 记忆轮数 |
| 密钥 | `src-tauri/src/secret.rs` | DPAPI（手写 FFI）加密存 `llm-key.bin`；日志只出现打码形态 |
| 记忆 | `src-tauri/src/memory.rs` | `memory.json` 全量保存 + 最近 N 轮进上下文；损坏先备份再重建；写盘串行化 |
| 碎碎念 | `src-tauri/src/whisper.rs` + `runtime.ts::onWhisper` | 宿主定时生成 → 事件 → 气泡 10 秒 + 可选 whisper 动画 |
| 设置页 | `settings.html` / `settings-page.ts` 的「AI」页 | 开关、provider、模型、Key 保存/清除、自检、清空记忆、隐私说明 |
| 验证工具 | `src-tauri/src/bin/mock-llm.rs` + `WHALE_PET_DIAG_LLM` | 本地 mock 服务端（零依赖）+ 进程内探针 |

## 13.2 默认零联网（红线）

原始配置（**没有 `llm` 段**）启动，三个 AI 动作全部被拒且**一个请求都没发**：

```text
[whale-pet][LLM 探针] 动作=all provider=deepseek baseUrl=https://api.deepseek.com model=deepseek-chat enabled=false
[whale-pet][LLM 探针] 自检失败（disabled）：AI 功能还没开启（设置 → AI）
[whale-pet][LLM 探针] 碎碎念失败（disabled）：AI 功能还没开启（设置 → AI）
[whale-pet][LLM 探针] 对话失败（disabled）：AI 功能还没开启（设置 → AI）
```

对照 mock 服务端的日志：这一轮**没有收到任何连接** ✓。

> 顺带说明选型：请求在 **Rust 侧**发（页面 `fetch` 会被各家 API 的 CORS 挡住），
> 于是页面 CSP **不需要放行任何外部域名**——少一个安全口子。

## 13.3 端到端（对着本地 mock，不需要真实 key）

`cargo run --bin mock-llm` + `provider=ollama`（本地模型不需要 key）+ `baseUrl` 指向 mock：

```text
[whale-pet][LLM 探针] 自检成功：（mock 回复）收到 2 条消息
[whale-pet][LLM 探针] 碎碎念成功：（mock 回复）收到 2 条消息
[whale-pet][LLM 探针] 对话成功：（mock 回复）收到 2 条消息
[whale-pet][LLM 探针] 记忆文件 …\memory.json：2 条（最近一轮：Some("你好呀，今天过得怎么样？") …）
```

**mock 侧把收到的 prompt 原文打了出来**，这是"请求组装是否符合设计"最直接的证据
（人设 + 名字声明 + 历史 + 本轮）：

```text
[mock-llm] POST /chat/completions
[mock-llm]   model=mock
[mock-llm]   system: 你是主人桌面上的Q版小鲸鱼桌宠，会时不时碎碎念一句。…不要提你是AI。
[mock-llm]   system: 你的名字是“小鲸鱼”。
[mock-llm]   user: 随便说一句日常碎碎念，一句就好，20 字以内。     ← 碎碎念
…
[mock-llm]   user: 你好呀，今天过得怎么样？                        ← 对话第一轮
[mock-llm]   assistant: （mock 回复）收到 2 条消息
[mock-llm]   user: 你好呀，今天过得怎么样？                        ← 对话第二轮（带上了上一轮）
```

第二轮 mock 收到 **4 条消息**（system + user + assistant + user）→ "最近 N 轮进上下文"成立；
`memory.json` 也从 2 条涨到 4 条 ✓。

## 13.4 失败分支（逐条实测）

mock 端按路径提供 5 种坏行为，逐条验证适配层的错误映射与中文提示：

| 场景 | 探针输出 |
| --- | --- |
| 401 | `自检失败（unauthorized）：API key 无效或没有权限（401/403）` |
| 429 | `自检失败（rate-limited）：请求太频繁或额度用尽（429）` |
| 200 但内容为空 | `自检失败（bad-response）：…模型未返回文本` |
| 200 但不是 JSON | `自检失败（bad-response）：…响应不是合法 JSON：expected value at line 1 column 1` |
| 慢响应 + `timeoutSec=5` | `自检失败（timeout）：模型服务响应超时` |
| `deepseek` 但没存 key | `自检失败（no-key）：还没有填 API key（设置 → AI）`（**未发请求**） |
| 总开关关闭 | `自检失败（disabled）：AI 功能还没开启（设置 → AI）`（**未发请求**） |

## 13.5 密钥安全（走真实 UI）

探针 `WHALE_PET_DIAG_SETTINGS=llmkeysave` 做的事与用户完全一致：**切到 AI 页 → 填 key →
点「保存密钥」→ 点「开始自检」**。证据：

```text
[whale-pet] 已保存 API key（sk-…6789（共 24 字符），DPAPI 加密后 246 字节）   ← 日志里是打码形态
密钥文件存在=True；文件里含明文 sk-mock-probe：False                          ← 落盘是密文
配置里含明文：False                                                          ← 配置里根本没有 key
```

设置页底部状态栏同时显示了自检结果（截图 [`screenshots/settings-ai.png`](screenshots/README.md)）：

```text
自检通过，模型回复：（mock 回复）收到 2 条消息
```

## 13.6 纯逻辑冒烟：60 → 88 项

新增 28 项覆盖"不需要网络"的部分：配置校验的 6 条拒绝路径、provider 预置值与"是否需要 key"、
system prompt 与消息顺序、请求体（含"不传 maxTokens"）、响应解析的 5 种形状、
DPAPI 往返 + 落盘无明文 + `mask` 不泄露、记忆的全量保存 / 最近 N 轮截断 / 损坏自愈与备份。

**这一段抓到一个真 bug（会直接导致应用起不来）**：
`llm: LlmConfig` 用了 `#[serde(default)]`，而字段级默认值是另一套；`serde` 在字段缺失时调的是
`Default::default()`，于是"老配置没有 llm 段"会得到 **空 provider → 校验失败 → 启动 panic**。
现在三个 `Default` 手写实现统一改成"从 `{}` 反序列化"，默认值只剩一处真相。

## 13.7 当时的未完成项（已在 13.8/13.9 补齐，保留记录）

| 项 | 状态 |
| --- | --- |
| 对话的**输入界面**（宠物角上的单行输入框） | 后端 `llm_chat` + 记忆已通并验证；输入窗（独立可聚焦小窗）未做 —— 用户目前只能靠探针/自检触发 |
| 表情包配图 / 余额 | 未开始（M3 后续项） |
| 真实 provider 联调 | 需要你给一个 Key（或本机跑 Ollama）；mock 已把除"真实模型输出质量"以外的链路全部验过 |
## 13.8 对话输入窗（本轮补齐）

后端 `llm_chat` 在上一轮已通，这一轮把**入口**补齐：宠物右上角的单行输入窗。

| 设计点 | 说明 |
| --- | --- |
| 为什么是独立小窗 | 宠物窗尺寸**严格等于**包围盒（`WINDOW_MARGIN_RATIO = 0`），里面没地方放输入框；而且命中区之外整窗穿透，放那儿的输入框点不动 |
| 唯一的可聚焦辅助窗 | 宠物/气泡/菜单/托盘菜单都 `focusable(false)`；输入框必须收键盘，所以它是**唯一**的例外（打开时主动 `set_focus()`） |
| 回复去哪了 | **不在输入框里堆聊天记录**，而是交给宠物页的气泡（与碎碎念同一条链路 `whisper::emit`）——与上游一致，也符合"桌宠"的形态 |
| 两个入口 | 托盘菜单「说两句…」+ 宠物右键菜单「工具 → 说两句…」 |
| 稳定性 | 对话窗（`chat-<宠物标签>`）也纳入了热重载的拆除清单，配置改动后不会留下孤儿窗 |

**证据（探针 `WHALE_PET_DIAG_CHAT=<毫秒>[:消息]`，走的是用户同一条路）**：

```text
[whale-pet] 对话窗已创建（隐藏）chat-pet-main-0：380×54
[whale-pet][对话探针] 已填入并点击发送：你好呀，今天过得怎么样？
[chat]      对话: 已回复（16ms）：（mock 回复）收到 2 条消息
[pet-main-0] 碎碎念: （mock 回复）收到 2 条消息        ← 回复走气泡，不在输入框里
memory.json：user「你好呀，今天过得怎么样？」→ assistant「（mock 回复）收到 2 条消息」
mock 侧：system = 人设 + 你的名字是“小鲸鱼”；user = 你好呀，今天过得怎么样？
```

**摆位账目**（加了一行诊断日志，位置这种事必须能对上账）：

```text
对话窗摆位输入：包围盒=(828,100) 命中区=(131,33 158×187) 期望=(1125,71) 夹取后=(892,71) 工作区=(0,0 1272×1273)
```

期望值贴命中区右上角；当时屏幕工作区只有 1272 宽，故被正确夹到 892——**窗口完整留在屏内**。
截图见 [`screenshots/chat-window.png`](screenshots/README.md)（输入条 + 回车发送/Esc 关闭提示）。

## 13.9 release 二进制回归

release 是 GUI 子系统（没有 stderr），所以证据改用**前端日志 + 落盘文件 + mock 侧**三处：

```text
[chat]       对话: 已回复（9ms）：（mock 回复）收到 2 条消息
[pet-main-0] 碎碎念: （mock 回复）收到 2 条消息
memory.json：user「release 回归：你在吗？」→ assistant「（mock 回复）收到 2 条消息」
mock 侧：POST /chat/completions（碎碎念 prompt）+ POST /chat/completions（release 回归：你在吗？）
```

即：**release 构建下**，自检、碎碎念、对话输入窗、记忆落盘、气泡显示全部正常。

## 13.10 第一阶段完成情况

| 目标项 | 状态 |
| --- | --- |
| ① LLM 适配层（多 provider / 统一构造与错误处理 / Rust 侧 HTTPS） | ✅ 已落地并验证 |
| ② API key 加密保存（DPAPI，无重型依赖，日志与配置无明文） | ✅ 走真实 UI 验证 |
| ③ 碎碎念（周期生成 → 气泡） | ✅ 定时器 + 气泡 10s + 可选动画 |
| ④ 对话（输入窗 + `memory.json` 全量记忆 + 最近 N 轮） | ✅ 本轮补齐输入窗并验证 |
| ⑤ 默认关闭联网 + 设置页开关/provider/模型/自检 | ✅ 原始配置零请求；AI 页可配可自检 |
| ⑥ 本地 mock 端到端 + `VERIFICATION` 记录 + release 回归 | ✅ 本节与 13.2~13.9 |

**未做（M3 后续）**：表情包配图、余额查询、真实 provider 的联调（需要用户提供 Key 或本机 Ollama）。
## 13.11 真实 provider 联调（DeepSeek）：抓到一个 mock 验不出的致命坑

用户提供了 DeepSeek key，于是做了**两次**真实调用（刻意只花两次请求：定时碎碎念关掉、
不跑循环；两次都是最小提示词）。

### 抓到的问题：https 传输层会 panic（release 下等于崩溃）

第一次真实请求直接炸在 ureq 内部：

```text
thread '<unnamed>' panicked at ureq-3.4.2/src/unversioned/transport/mod.rs:485:17:
uri scheme is https, provider is Rustls but feature is not enabled: rustls
```

原因：**ureq 3 默认的 TLS provider 是 Rustls**，而我们只开了 `native-tls` feature
（Windows 走 schannel，不需要 C 工具链）。feature 只是"可用"，**不会**自动切换默认 provider。
后果比"请求失败"严重得多：release 是 `panic = "abort"`，这条 panic 会**把整个应用带走**。

**关键教训**：本地 mock 是 `http://`，所以 mock 端到端**永远测不出**这一类传输层问题。
"能不花真钱就验完所有链路"这句话有边界——**https/TLS 这一段必须真跑一次**。

### 修复

`llm.rs` 里显式指定 provider（并写明为什么）：

```rust
let tls = ureq::tls::TlsConfig::builder()
    .provider(ureq::tls::TlsProvider::NativeTls)
    .build();
let agent: ureq::Agent = ureq::Agent::config_builder()
    .timeout_global(Some(Duration::from_secs(self.config.timeout_sec)))
    .tls_config(tls)
    .build()
    .into();
```

**回归守卫**（冒烟新增第 89 项）：对着一个必定连不上的 https 端口发一次请求，
要求"返回结构化失败且**不 panic**"——真 panic 会把冒烟进程直接带走，等于测试失败。
旧的代码在这一项上必然炸，所以它能挡住回归。

### 修复后的两次真实调用（DeepSeek `deepseek-chat`）

| 调用 | 结果 |
| --- | --- |
| 自检（碎碎念提示词） | `自检成功：好像该换水了，咕噜咕噜~` |
| 对话（"你叫什么名字？"） | `对话: 已回复（488ms）：我是小鲸鱼呀，嘿嘿～`，同时 `[pet-main-0] 碎碎念: 我是小鲸鱼呀，嘿嘿～`（**走气泡**），`memory.json` 落下一轮 user/assistant |

两点值得注意（都说明配置生效了）：
- 回复是**中文、短句、符合人设**（内置人设要求 20 字以内、不提 AI）；
- 问"你叫什么名字"时它答"我是小鲸鱼"——来自 system prompt 里的
  `你的名字是“小鲸鱼”。`（说明名字声明这条链路真的被模型读到了）。

### 收尾状态

- **AI 保持关闭**（配置已还原成用户原来的样子），所以**没有待机消耗**；
- API key 仍在 DPAPI 加密库里（`llm-key.bin`），日志里只有打码形态；
- 要启用：设置 → AI → 勾总开关与碎碎念/对话 → provider 已是 DeepSeek（地址与模型留空即用预置）。
## 13.12 用户实测撞到的 bug：保存配置后对话窗不见了

**现象**（用户右键「工具 → 说两句…」）：

```text
[pet-main-0] 菜单: 打开对话输入窗
[pet-main-0] ERROR 对话: 打开输入窗失败 | 对话窗不存在（chat-pet-main-0），prepare 没跑到？
```

**根因**：辅助窗的"拆除清单"与"重建清单"没有成对。

- 我在同一轮里把 `chat-<宠物标签>` 加进了**拆除清单**（热重载时按前缀销毁气泡/菜单/对话窗——
  它们都按宠物标签派生，宠物重建后一律作废）；
- 但**重建清单**（`reload.rs` 第 4 步的"预备气泡窗/菜单窗"）**漏了对话窗**。

于是：保存一次配置（或手改 config.jsonc）→ 热重载 → 对话窗被拆掉且再也没回来 → 之后每次点开都失败。
**启动时**是好的（`setup` 里三处预建都写了），所以这个 bug 只在"保存过配置之后"才出现。

**修复（两层）**：

1. **补上重建**：`reload.rs` 的重建循环里加上 `chat_window::prepare`，并把"三行必须成组"
   这句话写在代码注释里（拆除按前缀、重建按同一份清单）；
2. **按需创建这条后路**：`chat_window::open` 发现窗口不在就自己 `prepare` 再开
   （与 `bubble::show` 同样的考虑）——预建只是"点了就出现"的优化，
   **任何一条重建路径漏了预备，功能也不该直接失效**，更不该把内部错误抛给用户看。

**验证**（真机复现 + 压力）：

```text
[whale-pet][托盘探针] 触发菜单动作：chat     ← 修复后连点 8 次，其中一次正好跨过重建
[whale-pet] 对话窗摆位 chat-pet-main-0：原点=(892,71) 尺寸=380×54
…
[whale-pet][热重载] 检测到配置文件被修改，开始应用
[whale-pet] 对话窗已创建（隐藏）chat-pet-main-0：380×54   ← 修复生效：旧代码这里什么都不做
[whale-pet][热重载] 已应用（配置文件被外部修改）：1 只宠物，用时 513ms
[whale-pet] 对话窗摆位 chat-pet-main-0：原点=(892,71) 尺寸=380×54   ← 重建之后照常打开
```

8 次打开全部成功，日志里**没有**"对话窗不存在"。

**教训（写给下次加辅助窗的人）**：新增一种按宠物派生的辅助窗时，有**四处**要一起改——
① `setup` 预建、② 热重载重建、③ 热重载拆除清单、④ 打开时的按需创建后路。
漏掉 ②③ 中任何一个，都会得到"平时好好的、保存配置后就坏"这种最难查的 bug。
## 13.13 用户要求：输入条可拖动 + 长度减少五分之一

### 改动

| 项 | 前 | 后 |
| --- | --- | --- |
| 输入窗宽度 | 380 | **304**（正好少五分之一） |
| 拖动 | 不可拖（拖动区=整个窗口的空白处也没有） | 输入条**左侧握柄**按住即可拖（系统接管） |

**为什么只有握柄能拖**：输入框要能选文本、发送按钮要能点；整条都当拖动区会让
"想选字却把窗口拖走了"。握柄是 16px 宽的一条，鼠标移上去变 `grab`。

### 踩到的坑：Tauri 2 的窗口 API 走权限系统

握柄写完第一次按下去，**窗口纹丝不动**，日志里是：

```text
[chat] 对话: 握柄按下，交给系统拖动
[chat] ERROR 对话: 拖动失败 | window.start_dragging not allowed.
        Permissions associated with this command: core:window:allow-start-dragging
```

`capabilities/default.json` 里原来只有 `core:event:default`，而且窗口列表里**也没有 `chat-*`**。
两处都要补（并按最小授权：只给 `core:window:allow-start-dragging` 这一条，
**不授予 `core:window:default`**——页面没有理由能关窗/改尺寸/最小化）：

```json
"windows": ["pet-*", "bubble-*", "menu-*", "chat-*", "settings"],
"permissions": ["core:event:default", "core:window:allow-start-dragging"]
```

> 教训：**页面直接调窗口 API** 的功能（拖动、改尺寸、置顶…）都要过 capabilities。
> 这类漏授权在本机 mock、冒烟检查、类型检查里**全都测不出来**，
> 只有真按下去那一刻才会以"运行时报错"的形式出现。写进 13.12 的"四处清单"里，
> 现在这条要加进去当第五处：**⑤ 新窗口的 capabilities 权限**。

### 验证：真实鼠标注入拖动

不是"看代码觉得对"，而是用 `SetCursorPos` + `mouse_event` 注入一次真实拖动
（先把对话窗设为前台，否则第一下只会激活窗口）：

```text
拖动前：(2256,71) 304x54
拖动后：(1996,221)
位移：Δx=-260 Δy=150（与注入的位移完全一致）

[chat] 对话: 握柄按下，交给系统拖动      ← 旧构建：紧接着 "拖动失败 | not allowed"
[chat] 对话: 握柄按下，交给系统拖动      ← 新构建：无报错，窗口跟着走
```

截图更新为 [`screenshots/chat-window.png`](screenshots/README.md)（304 宽，左侧可见六点握柄）。
---

# 14. v0.1.0 首次出包与发布（2026-09-19）

## 14.1 安装包：一直记在「未能产出」的那一项，解决了

之前一直写着"本机 Tauri CLI 下载打包器（NSIS/WiX）不通"。这次网络恢复后重试，**一次成功**，
两个安装包都产出来了：

```text
Info Patching whale-pet-desktop.exe with bundle type information: nsis
Running makensis to produce bundle\nsis\whale-pet_0.1.0_x64-setup.exe
Downloading https://github.com/wixtoolset/wix3/releases/download/wix3141rtm/wix314-binaries.zip
Info validating hash / extracting WIX
Running candle / light to produce bundle\msi\whale-pet_0.1.0_x64_en-US.msi
Finished 2 bundles at: …setup.exe（2.91 MB）… en-US.msi（4.13 MB）
```

复现命令（本机 CLI 不在 `node_modules` 里，走 `D:\tools\Tauri\nodejs\tauri.cmd`）：

```powershell
& 'D:\tools\Tauri\nodejs\tauri.cmd' build            # 出 exe + NSIS + MSI
& 'D:\tools\Tauri\nodejs\tauri.cmd' build --bundles nsis   # 只出 NSIS（改安装器配置时快很多）
```

## 14.2 顺带发现并修掉：安装向导用的是 NSIS 默认图标

从 `setup.exe` 里抽 256px 图标核对，发现是 **NSIS 自带的默认图形**，不是我们的桌宠。
补上配置后重新出包，再抽一次确认已经是我们的图标：

```jsonc
"bundle": { "windows": { "nsis": {
  "installerIcon": "icons/icon.ico",
  "uninstallerIcon": "icons/icon.ico"
} } }
```

## 14.3 发布

- 标签 **v0.1.0**（附注标签，含本版范围说明）已推到 `origin`
- GitHub Release：<https://github.com/clover-us/sakura/releases/tag/v0.1.0>
  - `whale-pet_0.1.0_x64-setup.exe`（NSIS，2.91 MB）
  - `whale-pet_0.1.0_x64_en-US.msi`（4.13 MB）
  - 两个资源的状态都是 `uploaded`（用 GitHub API 回读确认过）
- 版本号：`Cargo.toml` / `tauri.conf.json` / `package.json` 三处都是 `0.1.0`（无版本漂移）

> **v0.1.0 标签在创建后十分钟内移动过一次**（从 `f40231d` 到 `b43cd9a`），
> 因为安装包图标修复应当属于这个版本。标签刚建、没有其他使用者，故直接 `-f` 移动并在标签说明里记了这件事；
> 正常流程不该这么做，这里写下来是为了不藏事。

## 14.4 安装包未验证的部分（诚实记录）

| 项 | 状态 |
| --- | --- |
| 安装包**能否装成功、装完能否启动** | **未验证**：本机不能拿真实安装流程做回归（会把当前开发环境覆盖/写注册表）。已做的是：setup.exe 是有效 PE（`MZ`）、能抽出图标、体积合理；release 二进制本身已实跑（9.6） |
| 代码签名 | **没有签名**（本机无证书）→ 用户侧会看到 SmartScreen「未知发布者」，Release 说明里已写明 |
| 卸载是否干净 | 未验证（同上） |
| MSI 在非中文系统上的语言 | 出的是 `en-US.msi`；NSIS 那份带 `SimpChinese` + `English` 两种语言 |

下一步若要真正闭环，需要：一台干净机器（或虚拟机）实装一次 + 代码签名证书 + CI 出包流水线。
---

# 15. M3 收尾（一）：表情包配图（2026-09-19）

## 15.1 交付物

| 部件 | 位置 | 说明 |
| --- | --- | --- |
| 素材导入 | `scripts/import-animations.ps1 -All`（已有）+ 本次实际导入 | 上游 27 张 memes（4.63 MB）+ pic/fonts → 本机数据目录 |
| 表情包表 | `config/default-config.jsonc` 的 `memes` 段 | 27 条「名称 → 内容描述」，从上游配置逐条抽取 |
| 池与解析 | `src-tauri/src/memes.rs` | 池过滤（图真在 + 描述非空、按名有序）、随机抽、两种提示词、`[图:名称]` 解析 |
| 配置开关 | `llm.whisper.imageEnabled` / `llm.chat.imageEnabled` | 默认 **false**；设置页 AI 页有开关，一张图都没有时置灰并说明原因 |
| 生成链路 | `llm::Generation { text, meme }` | 碎碎念随机抽（描述注入提示词）；对话把清单交给模型选（末尾标记） |
| 气泡配图 | `bubble.rs` + `bubble.html` + `#bubble-image` | 尺寸估算含配图（148×76 → 308×276），`<img>` 绝对地址由宿主拼 |
| 设置页 | AI 页 + `llm_status.memesCount` | 显示"本机有 N 张表情包"，并据此启用/禁用配图开关 |

**两种配图策略（照抄上游）**：碎碎念是"随口一句"、没有上下文可选，所以**随机抽**一张并把描述注入提示词；
对话有语境，把**整张清单**交给模型，它用末尾的 `[图:名称]` 要图——**只在池内命中才采纳**，
幻觉名字一律忽略但**绝不吞正文**（剥标记只剥那一行，正文原样保留）。

## 15.2 端到端验证（本地 mock，不需要真实 key）

碎碎念（`WHALE_PET_DIAG_LLM=whisper`）——mock 侧收到的提示词：

```text
[mock-llm]   user: 随便说一句日常碎碎念，一句就好，20 字以内。
这次会配一张表情包一起显示，图的内容是：假如给我三天多模态（蒙眼版对比自信版「鲸伦凯勒」，适合渴望多模态、缺能力、被剥夺感官）。
请让这句话和这张图的情绪/场景自然契合，像是配合画面说出来的；不要描述画面本身。
```

应用侧：

```text
[pet-main-0] 碎碎念: （mock 回复）收到 2 条消息（配图 memes/%E6%B7%B1%E5%BA%A6%E7%9D%A1%E7%9C%A0.png）
[whale-pet]   气泡窗显示 bubble-pet-main-0：308×276      ← 纯文字气泡是 148×76，配图把它撑高了
[bubble]      配图已加载 384x384 memes/%E6%B7%B1%E5%BA%A6%E7%9D%A1%E7%9C%A0.png
```

对话（`WHALE_PET_DIAG_CHAT`）——mock 在回复末尾回一个 `[图:…]` 标记：

```text
[chat]        对话: 已回复（287ms）：（mock 回复）收到 2 条消息      ← 标记已被剥掉
[pet-main-0]  碎碎念: （mock 回复）收到 2 条消息（配图 memes/Ciallo.png）
[bubble]      配图已加载 384x384 memes/Ciallo.png
memory.json：user「给我来一张表情包」→ assistant「（mock 回复）收到 2 条消息」   ← 记忆里也是干净正文
```

mock 侧确认收到了 `[配图]` 指令与 27 行清单。冒烟新增 12 项（池过滤、有序性、随机抽、两种提示词措辞、
标记解析的 5 种情况、素材路径编码），**101/101 通过**。

## 15.3 这一轮抓到的两个真 bug（都是"静默失败"型）

**① 气泡页没有 `window.__PET_ENV__`** —— 我最初让注入脚本用 `__PET_ENV__.assetBaseUrl` 拼图片地址，
但那个环境只有宠物页（`index.html`）注入了；气泡页拿到 `undefined`，相对路径落回 dev server
（`http://127.0.0.1:1420/memes/...`）→ 404。
**修法**：绝对地址由**宿主**拼（`pet_protocol::asset_base_url()` 是平台差异的唯一真相），
页面不再重复判断平台。

**② 素材协议只服务 `/webm/`** —— `pet_protocol.rs` 里写死了只放行 `/webm/` 前缀，`/memes/` 直接被 400。
**修法**：改成**白名单数组**（`ALLOWED_SUBDIRS = ["webm", "memes"]`），保持"每加一类素材都要显式登记"
这道门，路径穿越防护（`sanitize_join`）原样不动；协议根路径的说明也跟着列出所有可用根。

> 两个 bug 都是**静默**的：泡泡里少一张图，没有报错、没有日志。所以顺手给 `<img>` 挂了
> `onload` / `onerror` 回报（`[bubble] 配图已加载 384x384 …` / `配图加载失败 …（src=…）`）——
> 这条诊断**当场就把上面两个问题指出来了**，现在也成了"配图到底渲染出来没有"的可 grep 证据。
> 教训：**跨窗口/跨协议的资源加载一定要有失败回报**，否则用户只会看到"功能没生效"。

## 15.4 未做 / 下一步

- **余额查询**（M3 收尾第二半）：provider 表（DeepSeek `/user/balance`、OpenCode `/zen/go/v1/usage`）、
  超时与退避重试、气泡展示 —— 下一轮做；
- 表情包的"用户自定义导入"目前靠手工往数据目录 memes/ 放图 + 配置里写描述；
  设置页可以做"选择图片 + 填描述"的入口（未做）；
- 配图的尺寸固定 168×168（素材是 384×384）；非方形素材会用 `object-fit: contain` 留白。